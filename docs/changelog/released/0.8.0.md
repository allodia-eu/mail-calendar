# 0.8.0 — 2026-09-08

## macos

Paste into: App Store Connect (macOS)

**English**

```
Allodia Mail & Calendar can now offer to become your default mail app, so mail links open here.
Change your mind any time in Settings → General.

Drag files onto a message you are writing to attach them. Drag a picture and you are asked whether
to show it in the message or send it as a file to download.

Change the subject when replying or forwarding. The field opens with the usual "Re:" or "Fwd:"
and is yours to edit before sending.

Paste a screenshot or a photo straight into a message and it appears where you are typing, sent
as part of the message rather than as a file to download.

Right-click while writing a message to cut, copy and paste, and to copy the address of a link in
the message you are replying to.

Add and edit contacts. Fill in a name, organisation, role, addresses and numbers, and save into any
address book your accounts can write to. Editing a person who is in two accounts asks which one you
mean, and changes nothing you did not touch.

Click a mail link on your Mac and Allodia Mail & Calendar opens a message ready to write,
addressed for you.

Scroll the calendar sideways with a trackpad, a mouse wheel or a finger, and it now runs straight
across the week boundary instead of stopping at Sunday, coming to rest on a whole day.

Pick several messages and deal with them together: archive, trash, mark read or unread, flag.
Ctrl-click and Shift-click on a computer, Select or a long press on a phone.

Set how often an event repeats: daily, weekly, monthly or yearly, every few days or weeks, on the
weekdays you pick. Choose whether it ends on a date, after a number of times, or never.

Set the name your mail goes out under. You are asked once when an account is added, and can change
it any time in Settings, so your messages arrive as your name rather than just your address.

Closing an event with Cancel or Save no longer reopens it straight away.

Editing a repeating event from a later occurrence no longer deletes the ones before it, and
changing the time there now moves the whole series by that much.

HTML messages show their own layout, fonts and inline images again, instead of plain unstyled text.

Moving between messages no longer flashes a dark panel where the message should be; the page stays put and only the message on it changes.
```

**Nederlands**

```
Allodia Mail & Calendar kan nu aanbieden om je standaard e-mailapp te worden, zodat
e-mailkoppelingen hier openen. Je wijzigt dit wanneer je wilt in Instellingen → Algemeen.

Sleep bestanden op een bericht dat je schrijft om ze bij te voegen. Sleep je een afbeelding, dan
vragen we of je hem in het bericht wilt tonen of als bestand wilt meesturen.

Pas het onderwerp aan bij beantwoorden of doorsturen. Het veld begint met het gebruikelijke "Re:"
of "Fwd:" en je kunt het aanpassen voor je verzendt.

Plak een schermafbeelding of foto rechtstreeks in een bericht: hij verschijnt waar je typt en
gaat als onderdeel van het bericht mee, niet als bestand om te downloaden.

Klik met rechts terwijl je schrijft om te knippen, kopiëren en plakken, en om het adres te
kopiëren van een link in het bericht dat je beantwoordt.

Contacten toevoegen en wijzigen. Vul een naam, organisatie, functie, adressen en nummers in en sla
op in elk adresboek waar je accounts naartoe kunnen schrijven. Bij een contact in twee accounts
vraagt de app welke je bedoelt, en verandert er niets wat je niet hebt aangeraakt.

Klik op een e-mailkoppeling op je Mac en Allodia Mail & Calendar opent een bericht dat klaarstaat
om te schrijven, met de geadresseerde er al in.

Scroll de agenda zijwaarts met een trackpad, muiswiel of vinger: hij loopt nu gewoon door over de
weekgrens heen in plaats van bij zondag te stoppen, en komt op een hele dag tot stilstand.

Kies meerdere berichten en handel ze samen af: archiveren, weggooien, als gelezen of ongelezen
markeren, markeren. Ctrl-klik en Shift-klik op een computer, Selecteren of lang indrukken op een
telefoon.

Stel in hoe vaak een afspraak zich herhaalt: dagelijks, wekelijks, maandelijks of jaarlijks, om de
zoveel dagen of weken, op de weekdagen die je kiest. Kies of hij stopt op een datum, na een aantal
keer, of nooit.

Stel in onder welke naam je e-mail wordt verzonden. De app vraagt het eenmalig bij het toevoegen
van een account en je past het altijd aan in Instellingen, zodat berichten met je naam aankomen in
plaats van alleen je adres.

Een afspraak sluiten met Annuleren of Bewaren opent hem niet langer meteen opnieuw.

Een terugkerende afspraak bewerken vanaf een latere herhaling verwijdert de eerdere niet meer, en
de tijd daar aanpassen verschuift voortaan de hele reeks met datzelfde verschil.

HTML-berichten tonen weer hun eigen opmaak, lettertypen en ingesloten afbeeldingen, in plaats van kale tekst zonder opmaak.

Als je van bericht wisselt zie je niet langer kort een donker vlak waar het bericht hoort te staan; de pagina blijft staan en alleen het bericht erop verandert.
```

**Deutsch**

```
Allodia Mail & Calendar kann sich jetzt als Ihre Standard-E-Mail-App anbieten, damit E-Mail-Links
hier geöffnet werden. Sie ändern das jederzeit unter Einstellungen → Allgemein.

Ziehen Sie Dateien auf eine Nachricht, die Sie schreiben, um sie anzuhängen. Bei einem Bild fragen
wir, ob es im Text erscheinen oder als Datei mitgehen soll.

Ändern Sie den Betreff beim Antworten oder Weiterleiten. Das Feld beginnt mit dem üblichen "Re:"
oder "Fwd:" und lässt sich vor dem Senden bearbeiten.

Fügen Sie ein Bildschirmfoto oder Foto direkt in eine Nachricht ein: Es erscheint dort, wo Sie
schreiben, und geht als Teil der Nachricht mit statt als Datei zum Herunterladen.

Klicken Sie beim Schreiben mit der rechten Maustaste, um auszuschneiden, zu kopieren und
einzufügen und um die Adresse eines Links in der beantworteten Nachricht zu kopieren.

Kontakte hinzufügen und bearbeiten. Tragen Sie Name, Organisation, Position, Adressen und Nummern
ein und sichern Sie in jedes Adressbuch, in das Ihre Konten schreiben dürfen. Bei einem Kontakt in
zwei Konten fragt die App, welches Sie meinen, und ändert nichts, was Sie nicht angefasst haben.

Klicken Sie auf Ihrem Mac auf einen E-Mail-Link, und Allodia Mail & Calendar öffnet eine Nachricht,
die bereits adressiert ist und auf Ihren Text wartet.

Scrollen Sie den Kalender mit Trackpad, Mausrad oder Finger zur Seite: Er läuft jetzt über die
Wochengrenze hinweg, statt am Sonntag zu stoppen, und kommt auf einem ganzen Tag zur Ruhe.

Wählen Sie mehrere Nachrichten aus und erledigen Sie sie zusammen: archivieren, löschen, als gelesen
oder ungelesen markieren, markieren. Strg-Klick und Umschalt-Klick am Computer, auf dem Telefon
Auswählen oder langes Drücken.

Legen Sie fest, wie oft sich ein Termin wiederholt: täglich, wöchentlich, monatlich oder jährlich,
alle paar Tage oder Wochen, an den von Ihnen gewählten Wochentagen. Wählen Sie, ob er an einem
Datum, nach einer Anzahl von Malen oder nie endet.

Legen Sie fest, unter welchem Namen Ihre E-Mails versendet werden. Die App fragt einmal beim
Hinzufügen eines Kontos danach, und Sie ändern ihn jederzeit in den Einstellungen, damit Ihre
Nachrichten mit Ihrem Namen statt nur Ihrer Adresse ankommen.

Wer einen Termin mit Abbrechen oder Sichern schließt, bekommt ihn nicht mehr sofort wieder
geöffnet.

Wer einen Serientermin von einem späteren Termin aus bearbeitet, verliert die früheren nicht mehr,
und eine dort geänderte Uhrzeit verschiebt jetzt die ganze Serie um denselben Betrag.

HTML-Nachrichten zeigen wieder ihr eigenes Layout, ihre Schriften und eingebetteten Bilder statt reinem Text ohne Gestaltung.

Beim Wechsel zwischen Nachrichten blitzt keine dunkle Fläche mehr dort auf, wo die Nachricht stehen sollte; die Seite bleibt stehen und nur die Nachricht darauf wechselt.
```

**Français**

```
Allodia Mail & Calendar peut désormais proposer de devenir votre application de messagerie par
défaut, pour que les liens e-mail s'ouvrent ici. Modifiable à tout moment dans Réglages → Général.

Faites glisser des fichiers sur un message en cours d'écriture pour les joindre. Pour une image,
nous demandons si elle doit apparaître dans le message ou partir en pièce jointe.

Modifiez l'objet lors d'une réponse ou d'un transfert. Le champ s'ouvre avec le « Re: » ou
« Fwd: » habituel et reste modifiable avant l'envoi.

Collez une capture d'écran ou une photo directement dans un message : elle apparaît là où vous
écrivez et part avec le message plutôt qu'en pièce jointe à télécharger.

Faites un clic droit pendant que vous écrivez pour couper, copier et coller, et pour copier
l'adresse d'un lien du message auquel vous répondez.

Ajoutez et modifiez des contacts. Saisissez un nom, une organisation, une fonction, des adresses et
des numéros, puis enregistrez dans n'importe quel carnet d'adresses où vos comptes peuvent écrire.
Pour un contact présent dans deux comptes, l'app demande lequel vous visez, et ne change rien
d'autre.

Cliquez sur un lien e-mail sur votre Mac et Allodia Mail & Calendar ouvre un message prêt à écrire,
déjà adressé.

Faites défiler le calendrier latéralement avec un pavé tactile, une molette ou un doigt : il passe
désormais la limite de la semaine au lieu de s’arrêter au dimanche, et s’arrête sur un jour entier.

Sélectionnez plusieurs messages et traitez-les ensemble : archiver, mettre à la corbeille, marquer
comme lu ou non lu, signaler. Ctrl-clic et Maj-clic sur un ordinateur, Sélectionner ou appui long
sur un téléphone.

Définissez la fréquence de répétition d’un événement : chaque jour, semaine, mois ou année, tous
les quelques jours ou semaines, les jours que vous choisissez. Choisissez s’il se termine à une
date, après un certain nombre de fois, ou jamais.

Choisissez le nom sous lequel vos messages partent. L'app vous le demande une fois lors de l'ajout
d'un compte et vous le modifiez quand vous voulez dans les Réglages, pour que vos messages arrivent
à votre nom plutôt qu'à votre seule adresse.

Fermer un événement avec Annuler ou Enregistrer ne le rouvre plus aussitôt.

Modifier un événement récurrent depuis une occurrence ultérieure ne supprime plus les précédentes,
et y changer l’heure décale désormais toute la série d’autant.

Les messages HTML retrouvent leur mise en page, leurs polices et leurs images intégrées, au lieu d'un texte brut sans mise en forme.

Passer d'un message à l'autre ne fait plus apparaître brièvement un fond sombre à la place du message ; la page reste en place et seul le message qui s'y trouve change.
```

**Español**

```
Allodia Mail & Calendar ahora puede ofrecerse como tu aplicación de correo predeterminada, para que
los enlaces de correo se abran aquí. Cámbialo cuando quieras en Ajustes → General.

Arrastra archivos al mensaje que estás escribiendo para adjuntarlos. Con una imagen te
preguntamos si quieres mostrarla en el mensaje o enviarla como archivo.

Cambia el asunto al responder o reenviar. El campo se abre con el "Re:" o "Fwd:" habitual y
puedes editarlo antes de enviar.

Pega una captura de pantalla o una foto directamente en un mensaje: aparece donde estás
escribiendo y se envía dentro del mensaje, no como archivo para descargar.

Haz clic derecho mientras escribes para cortar, copiar y pegar, y para copiar la dirección de un
enlace del mensaje al que respondes.

Añade y edita contactos. Escribe un nombre, una organización, un cargo, direcciones y números, y
guarda en cualquier libreta de direcciones en la que tus cuentas puedan escribir. Si el contacto
está en dos cuentas, la app te pregunta a cuál te refieres y no cambia nada que no hayas tocado.

Haz clic en un enlace de correo en tu Mac y Allodia Mail & Calendar abre un mensaje listo para
escribir, ya dirigido.

Desplaza el calendario en horizontal con el trackpad, la rueda del ratón o el dedo: ahora cruza el
límite de la semana en vez de pararse en domingo, y se detiene en un día completo.

Seleccione varios mensajes y resuélvalos juntos: archivar, mover a la papelera, marcar como leído o
no leído, marcar. Ctrl-clic y Mayús-clic en un ordenador, Seleccionar o una pulsación larga en un
teléfono.

Define con qué frecuencia se repite un evento: cada día, semana, mes o año, cada pocos días o
semanas, en los días que elijas. Elige si termina en una fecha, después de un número de veces, o
nunca.

Elige el nombre con el que sale tu correo. La app te lo pregunta una vez al añadir una cuenta y
puedes cambiarlo cuando quieras en Ajustes, para que tus mensajes lleguen con tu nombre y no solo
con tu dirección.

Cerrar un evento con Cancelar o Guardar ya no vuelve a abrirlo de inmediato.

Editar un evento periódico desde una repetición posterior ya no elimina las anteriores, y cambiar
la hora allí ahora desplaza toda la serie en la misma medida.

Los mensajes HTML vuelven a mostrar su propio diseño, sus tipografías y sus imágenes insertadas, en lugar de texto sin formato.

Al pasar de un mensaje a otro ya no aparece un instante un panel oscuro donde debería estar el mensaje; la página se mantiene y solo cambia el mensaje que hay en ella.
```

**Italiano**

```
Allodia Mail & Calendar può ora proporsi come app di posta predefinita, così i link e-mail si
aprono qui. Modificabile in qualsiasi momento in Impostazioni → Generali.

Trascina i file sul messaggio che stai scrivendo per allegarli. Per un'immagine ti chiediamo se
mostrarla nel messaggio o inviarla come file.

Cambia l'oggetto quando rispondi o inoltri. Il campo si apre con il solito "Re:" o "Fwd:" e resta
modificabile prima dell'invio.

Incolla uno screenshot o una foto direttamente in un messaggio: compare dove stai scrivendo e
viene inviata dentro il messaggio, non come file da scaricare.

Fai clic destro mentre scrivi per tagliare, copiare e incollare, e per copiare l'indirizzo di un
link nel messaggio a cui stai rispondendo.

Aggiungi e modifica i contatti. Inserisci nome, organizzazione, ruolo, indirizzi e numeri e salva in
qualsiasi rubrica su cui i tuoi account possono scrivere. Se il contatto è in due account, l'app
chiede quale intendi e non cambia nulla che tu non abbia toccato.

Fai clic su un link e-mail sul tuo Mac e Allodia Mail & Calendar apre un messaggio pronto da
scrivere, già indirizzato.

Scorri il calendario in orizzontale con trackpad, rotellina o dito: ora prosegue oltre il confine
della settimana invece di fermarsi alla domenica, e si ferma su un giorno intero.

Seleziona più messaggi e gestiscili insieme: archivia, sposta nel cestino, segna come letto o non
letto, contrassegna. Ctrl-clic e Maiusc-clic su un computer, Seleziona o una pressione prolungata su
un telefono.

Imposta la frequenza con cui un evento si ripete: ogni giorno, settimana, mese o anno, ogni pochi
giorni o settimane, nei giorni che scegli. Scegli se termina in una data, dopo un certo numero di
volte, o mai.

Scegli il nome con cui parte la tua posta. L'app te lo chiede una volta quando aggiungi un account
e puoi cambiarlo quando vuoi nelle Impostazioni, così i messaggi arrivano con il tuo nome e non
solo con il tuo indirizzo.

Chiudere un evento con Annulla o Salva non lo riapre più subito.

Modificare un evento ricorrente da un'occorrenza successiva non elimina più quelle precedenti, e
cambiare l'ora lì ora sposta l'intera serie della stessa quantità.

I messaggi HTML mostrano di nuovo il proprio impaginato, i caratteri e le immagini incorporate, invece di testo senza formattazione.

Passando da un messaggio all'altro non compare più per un istante un riquadro scuro al posto del messaggio; la pagina resta ferma e cambia solo il messaggio su di essa.
```

**Português**

```
O Allodia Mail & Calendar pode agora propor-se como a sua aplicação de e-mail predefinida, para que
as ligações de e-mail abram aqui. Altere quando quiser em Definições → Geral.

Arraste ficheiros para a mensagem que está a escrever para os anexar. No caso de uma imagem,
perguntamos se quer mostrá-la na mensagem ou enviá-la como ficheiro.

Altere o assunto ao responder ou reencaminhar. O campo abre com o habitual "Re:" ou "Fwd:" e pode
ser editado antes de enviar.

Cole uma captura de ecrã ou uma fotografia diretamente numa mensagem: aparece onde está a
escrever e segue dentro da mensagem, não como ficheiro para transferir.

Clique com o botão direito enquanto escreve para cortar, copiar e colar, e para copiar o endereço
de uma ligação da mensagem a que está a responder.

Adicione e edite contactos. Introduza um nome, uma organização, um cargo, endereços e números e
guarde em qualquer livro de endereços onde as suas contas possam escrever. Num contacto que esteja
em duas contas, a aplicação pergunta qual pretende e não altera nada em que não tenha tocado.

Clique num link de e-mail no seu Mac e o Allodia Mail & Calendar abre uma mensagem pronta a
escrever, já endereçada.

Percorra o calendário na horizontal com o trackpad, a roda do rato ou o dedo: agora atravessa o
limite da semana em vez de parar no domingo, e para num dia inteiro.

Selecione várias mensagens e trate-as em conjunto: arquivar, mover para o lixo, marcar como lida ou
não lida, sinalizar. Ctrl-clique e Shift-clique num computador, Selecionar ou toque longo num
telemóvel.

Defina com que frequência um evento se repete: todos os dias, semanas, meses ou anos, a cada poucos
dias ou semanas, nos dias que escolher. Escolha se termina numa data, após um número de vezes, ou
nunca.

Defina o nome com que o seu correio é enviado. A aplicação pergunta uma vez ao adicionar uma conta
e pode alterá-lo quando quiser nas Definições, para que as mensagens cheguem com o seu nome e não
apenas com o seu endereço.

Fechar um evento com Cancelar ou Guardar deixa de o reabrir de imediato.

Editar um evento recorrente a partir de uma ocorrência posterior deixa de apagar as anteriores, e
alterar a hora aí passa a deslocar toda a série na mesma medida.

As mensagens HTML voltam a mostrar o seu próprio grafismo, tipos de letra e imagens incorporadas, em vez de texto sem qualquer formatação.

Ao mudar de mensagem deixa de surgir por instantes uma área escura no lugar da mensagem; a página mantém-se e muda apenas a mensagem que está nela.
```

## ios

Paste into: App Store Connect (iOS/iPadOS)

**English**

```
Drag files onto a message you are writing to attach them. Drag a picture and you are asked whether
to show it in the message or send it as a file to download.

Change the subject when replying or forwarding. The field opens with the usual "Re:" or "Fwd:"
and is yours to edit before sending.

Paste a screenshot or a photo straight into a message and it appears where you are typing, sent
as part of the message rather than as a file to download.

Add and edit contacts. Fill in a name, organisation, role, addresses and numbers, and save into any
address book your accounts can write to. Editing a person who is in two accounts asks which one you
mean, and changes nothing you did not touch.

Scroll the calendar sideways with a trackpad, a mouse wheel or a finger, and it now runs straight
across the week boundary instead of stopping at Sunday, coming to rest on a whole day.

Pick several messages and deal with them together: archive, trash, mark read or unread, flag.
Ctrl-click and Shift-click on a computer, Select or a long press on a phone.

Set how often an event repeats: daily, weekly, monthly or yearly, every few days or weeks, on the
weekdays you pick. Choose whether it ends on a date, after a number of times, or never.

Set the name your mail goes out under. You are asked once when an account is added, and can change
it any time in Settings, so your messages arrive as your name rather than just your address.

Closing an event with Cancel or Save no longer reopens it straight away.

Editing a repeating event from a later occurrence no longer deletes the ones before it, and
changing the time there now moves the whole series by that much.

HTML messages show their own layout, fonts and inline images again, instead of plain unstyled text.

Moving between messages no longer flashes a dark panel where the message should be; the page stays put and only the message on it changes.
```

**Nederlands**

```
Sleep bestanden op een bericht dat je schrijft om ze bij te voegen. Sleep je een afbeelding, dan
vragen we of je hem in het bericht wilt tonen of als bestand wilt meesturen.

Pas het onderwerp aan bij beantwoorden of doorsturen. Het veld begint met het gebruikelijke "Re:"
of "Fwd:" en je kunt het aanpassen voor je verzendt.

Plak een schermafbeelding of foto rechtstreeks in een bericht: hij verschijnt waar je typt en
gaat als onderdeel van het bericht mee, niet als bestand om te downloaden.

Contacten toevoegen en wijzigen. Vul een naam, organisatie, functie, adressen en nummers in en sla
op in elk adresboek waar je accounts naartoe kunnen schrijven. Bij een contact in twee accounts
vraagt de app welke je bedoelt, en verandert er niets wat je niet hebt aangeraakt.

Scroll de agenda zijwaarts met een trackpad, muiswiel of vinger: hij loopt nu gewoon door over de
weekgrens heen in plaats van bij zondag te stoppen, en komt op een hele dag tot stilstand.

Kies meerdere berichten en handel ze samen af: archiveren, weggooien, als gelezen of ongelezen
markeren, markeren. Ctrl-klik en Shift-klik op een computer, Selecteren of lang indrukken op een
telefoon.

Stel in hoe vaak een afspraak zich herhaalt: dagelijks, wekelijks, maandelijks of jaarlijks, om de
zoveel dagen of weken, op de weekdagen die je kiest. Kies of hij stopt op een datum, na een aantal
keer, of nooit.

Stel in onder welke naam je e-mail wordt verzonden. De app vraagt het eenmalig bij het toevoegen
van een account en je past het altijd aan in Instellingen, zodat berichten met je naam aankomen in
plaats van alleen je adres.

Een afspraak sluiten met Annuleren of Bewaren opent hem niet langer meteen opnieuw.

Een terugkerende afspraak bewerken vanaf een latere herhaling verwijdert de eerdere niet meer, en
de tijd daar aanpassen verschuift voortaan de hele reeks met datzelfde verschil.

HTML-berichten tonen weer hun eigen opmaak, lettertypen en ingesloten afbeeldingen, in plaats van kale tekst zonder opmaak.

Als je van bericht wisselt zie je niet langer kort een donker vlak waar het bericht hoort te staan; de pagina blijft staan en alleen het bericht erop verandert.
```

**Deutsch**

```
Ziehen Sie Dateien auf eine Nachricht, die Sie schreiben, um sie anzuhängen. Bei einem Bild fragen
wir, ob es im Text erscheinen oder als Datei mitgehen soll.

Ändern Sie den Betreff beim Antworten oder Weiterleiten. Das Feld beginnt mit dem üblichen "Re:"
oder "Fwd:" und lässt sich vor dem Senden bearbeiten.

Fügen Sie ein Bildschirmfoto oder Foto direkt in eine Nachricht ein: Es erscheint dort, wo Sie
schreiben, und geht als Teil der Nachricht mit statt als Datei zum Herunterladen.

Kontakte hinzufügen und bearbeiten. Tragen Sie Name, Organisation, Position, Adressen und Nummern
ein und sichern Sie in jedes Adressbuch, in das Ihre Konten schreiben dürfen. Bei einem Kontakt in
zwei Konten fragt die App, welches Sie meinen, und ändert nichts, was Sie nicht angefasst haben.

Scrollen Sie den Kalender mit Trackpad, Mausrad oder Finger zur Seite: Er läuft jetzt über die
Wochengrenze hinweg, statt am Sonntag zu stoppen, und kommt auf einem ganzen Tag zur Ruhe.

Wählen Sie mehrere Nachrichten aus und erledigen Sie sie zusammen: archivieren, löschen, als gelesen
oder ungelesen markieren, markieren. Strg-Klick und Umschalt-Klick am Computer, auf dem Telefon
Auswählen oder langes Drücken.

Legen Sie fest, wie oft sich ein Termin wiederholt: täglich, wöchentlich, monatlich oder jährlich,
alle paar Tage oder Wochen, an den von Ihnen gewählten Wochentagen. Wählen Sie, ob er an einem
Datum, nach einer Anzahl von Malen oder nie endet.

Legen Sie fest, unter welchem Namen Ihre E-Mails versendet werden. Die App fragt einmal beim
Hinzufügen eines Kontos danach, und Sie ändern ihn jederzeit in den Einstellungen, damit Ihre
Nachrichten mit Ihrem Namen statt nur Ihrer Adresse ankommen.

Wer einen Termin mit Abbrechen oder Sichern schließt, bekommt ihn nicht mehr sofort wieder
geöffnet.

Wer einen Serientermin von einem späteren Termin aus bearbeitet, verliert die früheren nicht mehr,
und eine dort geänderte Uhrzeit verschiebt jetzt die ganze Serie um denselben Betrag.

HTML-Nachrichten zeigen wieder ihr eigenes Layout, ihre Schriften und eingebetteten Bilder statt reinem Text ohne Gestaltung.

Beim Wechsel zwischen Nachrichten blitzt keine dunkle Fläche mehr dort auf, wo die Nachricht stehen sollte; die Seite bleibt stehen und nur die Nachricht darauf wechselt.
```

**Français**

```
Faites glisser des fichiers sur un message en cours d'écriture pour les joindre. Pour une image,
nous demandons si elle doit apparaître dans le message ou partir en pièce jointe.

Modifiez l'objet lors d'une réponse ou d'un transfert. Le champ s'ouvre avec le « Re: » ou
« Fwd: » habituel et reste modifiable avant l'envoi.

Collez une capture d'écran ou une photo directement dans un message : elle apparaît là où vous
écrivez et part avec le message plutôt qu'en pièce jointe à télécharger.

Ajoutez et modifiez des contacts. Saisissez un nom, une organisation, une fonction, des adresses et
des numéros, puis enregistrez dans n'importe quel carnet d'adresses où vos comptes peuvent écrire.
Pour un contact présent dans deux comptes, l'app demande lequel vous visez, et ne change rien
d'autre.

Faites défiler le calendrier latéralement avec un pavé tactile, une molette ou un doigt : il passe
désormais la limite de la semaine au lieu de s’arrêter au dimanche, et s’arrête sur un jour entier.

Sélectionnez plusieurs messages et traitez-les ensemble : archiver, mettre à la corbeille, marquer
comme lu ou non lu, signaler. Ctrl-clic et Maj-clic sur un ordinateur, Sélectionner ou appui long
sur un téléphone.

Définissez la fréquence de répétition d’un événement : chaque jour, semaine, mois ou année, tous
les quelques jours ou semaines, les jours que vous choisissez. Choisissez s’il se termine à une
date, après un certain nombre de fois, ou jamais.

Choisissez le nom sous lequel vos messages partent. L'app vous le demande une fois lors de l'ajout
d'un compte et vous le modifiez quand vous voulez dans les Réglages, pour que vos messages arrivent
à votre nom plutôt qu'à votre seule adresse.

Fermer un événement avec Annuler ou Enregistrer ne le rouvre plus aussitôt.

Modifier un événement récurrent depuis une occurrence ultérieure ne supprime plus les précédentes,
et y changer l’heure décale désormais toute la série d’autant.

Les messages HTML retrouvent leur mise en page, leurs polices et leurs images intégrées, au lieu d'un texte brut sans mise en forme.

Passer d'un message à l'autre ne fait plus apparaître brièvement un fond sombre à la place du message ; la page reste en place et seul le message qui s'y trouve change.
```

**Español**

```
Arrastra archivos al mensaje que estás escribiendo para adjuntarlos. Con una imagen te
preguntamos si quieres mostrarla en el mensaje o enviarla como archivo.

Cambia el asunto al responder o reenviar. El campo se abre con el "Re:" o "Fwd:" habitual y
puedes editarlo antes de enviar.

Pega una captura de pantalla o una foto directamente en un mensaje: aparece donde estás
escribiendo y se envía dentro del mensaje, no como archivo para descargar.

Añade y edita contactos. Escribe un nombre, una organización, un cargo, direcciones y números, y
guarda en cualquier libreta de direcciones en la que tus cuentas puedan escribir. Si el contacto
está en dos cuentas, la app te pregunta a cuál te refieres y no cambia nada que no hayas tocado.

Desplaza el calendario en horizontal con el trackpad, la rueda del ratón o el dedo: ahora cruza el
límite de la semana en vez de pararse en domingo, y se detiene en un día completo.

Seleccione varios mensajes y resuélvalos juntos: archivar, mover a la papelera, marcar como leído o
no leído, marcar. Ctrl-clic y Mayús-clic en un ordenador, Seleccionar o una pulsación larga en un
teléfono.

Define con qué frecuencia se repite un evento: cada día, semana, mes o año, cada pocos días o
semanas, en los días que elijas. Elige si termina en una fecha, después de un número de veces, o
nunca.

Elige el nombre con el que sale tu correo. La app te lo pregunta una vez al añadir una cuenta y
puedes cambiarlo cuando quieras en Ajustes, para que tus mensajes lleguen con tu nombre y no solo
con tu dirección.

Cerrar un evento con Cancelar o Guardar ya no vuelve a abrirlo de inmediato.

Editar un evento periódico desde una repetición posterior ya no elimina las anteriores, y cambiar
la hora allí ahora desplaza toda la serie en la misma medida.

Los mensajes HTML vuelven a mostrar su propio diseño, sus tipografías y sus imágenes insertadas, en lugar de texto sin formato.

Al pasar de un mensaje a otro ya no aparece un instante un panel oscuro donde debería estar el mensaje; la página se mantiene y solo cambia el mensaje que hay en ella.
```

**Italiano**

```
Trascina i file sul messaggio che stai scrivendo per allegarli. Per un'immagine ti chiediamo se
mostrarla nel messaggio o inviarla come file.

Cambia l'oggetto quando rispondi o inoltri. Il campo si apre con il solito "Re:" o "Fwd:" e resta
modificabile prima dell'invio.

Incolla uno screenshot o una foto direttamente in un messaggio: compare dove stai scrivendo e
viene inviata dentro il messaggio, non come file da scaricare.

Aggiungi e modifica i contatti. Inserisci nome, organizzazione, ruolo, indirizzi e numeri e salva in
qualsiasi rubrica su cui i tuoi account possono scrivere. Se il contatto è in due account, l'app
chiede quale intendi e non cambia nulla che tu non abbia toccato.

Scorri il calendario in orizzontale con trackpad, rotellina o dito: ora prosegue oltre il confine
della settimana invece di fermarsi alla domenica, e si ferma su un giorno intero.

Seleziona più messaggi e gestiscili insieme: archivia, sposta nel cestino, segna come letto o non
letto, contrassegna. Ctrl-clic e Maiusc-clic su un computer, Seleziona o una pressione prolungata su
un telefono.

Imposta la frequenza con cui un evento si ripete: ogni giorno, settimana, mese o anno, ogni pochi
giorni o settimane, nei giorni che scegli. Scegli se termina in una data, dopo un certo numero di
volte, o mai.

Scegli il nome con cui parte la tua posta. L'app te lo chiede una volta quando aggiungi un account
e puoi cambiarlo quando vuoi nelle Impostazioni, così i messaggi arrivano con il tuo nome e non
solo con il tuo indirizzo.

Chiudere un evento con Annulla o Salva non lo riapre più subito.

Modificare un evento ricorrente da un'occorrenza successiva non elimina più quelle precedenti, e
cambiare l'ora lì ora sposta l'intera serie della stessa quantità.

I messaggi HTML mostrano di nuovo il proprio impaginato, i caratteri e le immagini incorporate, invece di testo senza formattazione.

Passando da un messaggio all'altro non compare più per un istante un riquadro scuro al posto del messaggio; la pagina resta ferma e cambia solo il messaggio su di essa.
```

**Português**

```
Arraste ficheiros para a mensagem que está a escrever para os anexar. No caso de uma imagem,
perguntamos se quer mostrá-la na mensagem ou enviá-la como ficheiro.

Altere o assunto ao responder ou reencaminhar. O campo abre com o habitual "Re:" ou "Fwd:" e pode
ser editado antes de enviar.

Cole uma captura de ecrã ou uma fotografia diretamente numa mensagem: aparece onde está a
escrever e segue dentro da mensagem, não como ficheiro para transferir.

Adicione e edite contactos. Introduza um nome, uma organização, um cargo, endereços e números e
guarde em qualquer livro de endereços onde as suas contas possam escrever. Num contacto que esteja
em duas contas, a aplicação pergunta qual pretende e não altera nada em que não tenha tocado.

Percorra o calendário na horizontal com o trackpad, a roda do rato ou o dedo: agora atravessa o
limite da semana em vez de parar no domingo, e para num dia inteiro.

Selecione várias mensagens e trate-as em conjunto: arquivar, mover para o lixo, marcar como lida ou
não lida, sinalizar. Ctrl-clique e Shift-clique num computador, Selecionar ou toque longo num
telemóvel.

Defina com que frequência um evento se repete: todos os dias, semanas, meses ou anos, a cada poucos
dias ou semanas, nos dias que escolher. Escolha se termina numa data, após um número de vezes, ou
nunca.

Defina o nome com que o seu correio é enviado. A aplicação pergunta uma vez ao adicionar uma conta
e pode alterá-lo quando quiser nas Definições, para que as mensagens cheguem com o seu nome e não
apenas com o seu endereço.

Fechar um evento com Cancelar ou Guardar deixa de o reabrir de imediato.

Editar um evento recorrente a partir de uma ocorrência posterior deixa de apagar as anteriores, e
alterar a hora aí passa a deslocar toda a série na mesma medida.

As mensagens HTML voltam a mostrar o seu próprio grafismo, tipos de letra e imagens incorporadas, em vez de texto sem qualquer formatação.

Ao mudar de mensagem deixa de surgir por instantes uma área escura no lugar da mensagem; a página mantém-se e muda apenas a mensagem que está nela.
```

## windows

Paste into: Microsoft Store

Written, not assembled: 13 changes do not fit this store's cap. Every one of them is in the appendix below.

**English**

```
New: Allodia Mail & Calendar can offer to become your default mail app, so mail links open here.
Drag files onto a message to attach them, or paste a picture straight in. Edit the subject when
you reply or forward. Right-click while you write to cut, copy and paste. Add and edit contacts.
Select several messages and archive, trash, flag or mark them together. Set how often an event
repeats, and when it ends. Share a file in from any other app. Choose the name your mail goes out
under. Fixed: HTML mail renders with its own styling again, the reading pane no longer flashes
between messages, and editing a repeating event from a later occurrence keeps the ones before it.
```

**Nederlands**

```
Nieuw: Allodia Mail & Calendar kan aanbieden je standaard e-mailapp te worden, zodat
e-mailkoppelingen hier openen. Sleep bestanden op een bericht om ze bij te voegen, of plak een
afbeelding er rechtstreeks in. Pas het onderwerp aan bij beantwoorden of doorsturen. Klik met
rechts tijdens het schrijven om te knippen, kopiëren en plakken. Voeg contacten toe en wijzig ze.
Kies meerdere berichten en archiveer, verwijder of markeer ze samen. Stel in hoe vaak een afspraak
zich herhaalt, en wanneer die stopt. Deel een bestand vanuit een andere app. Kies de naam
waaronder je e-mail uitgaat. Opgelost: HTML-berichten tonen weer hun eigen opmaak, het leesvenster
flitst niet meer tussen berichten, en een reeks bewerken vanaf een latere herhaling behoudt de
eerdere.
```

**Deutsch**

```
Neu: Allodia Mail & Calendar kann sich als Ihre Standard-E-Mail-App anbieten, damit E-Mail-Links
hier geöffnet werden. Ziehen Sie Dateien auf eine Nachricht, um sie anzuhängen, oder fügen Sie ein
Bild direkt ein. Ändern Sie den Betreff beim Antworten oder Weiterleiten. Klicken Sie beim
Schreiben mit der rechten Maustaste, um auszuschneiden, zu kopieren und einzufügen. Kontakte
anlegen und bearbeiten. Mehrere Nachrichten auswählen und zusammen archivieren, löschen oder
markieren. Festlegen, wie oft ein Termin sich wiederholt und wann er endet. Eine Datei aus einer
anderen App teilen. Den Namen wählen, unter dem Ihre Mail versendet wird. Behoben:
HTML-Nachrichten zeigen wieder ihre eigene Gestaltung, das Lesefenster blitzt beim Wechseln nicht
mehr, und eine Serienänderung von einem späteren Termin aus behält die früheren.
```

**Français**

```
Nouveau : Allodia Mail & Calendar peut proposer de devenir votre application de messagerie par
défaut, pour que les liens e-mail s'ouvrent ici. Faites glisser des fichiers sur un message pour
les joindre, ou collez-y une image directement. Modifiez l'objet en réponse ou en transfert.
Faites un clic droit pendant que vous écrivez pour couper, copier et coller. Ajoutez et modifiez
des contacts. Sélectionnez plusieurs messages et archivez, supprimez ou marquez-les ensemble.
Définissez la fréquence d'un événement et sa fin. Partagez un fichier depuis une autre
application. Choisissez le nom sous lequel vos messages partent. Corrigé : les messages HTML
retrouvent leur mise en page, le volet de lecture ne clignote plus, et modifier une série depuis
une occurrence ultérieure conserve les précédentes.
```

**Español**

```
Nuevo: Allodia Mail & Calendar puede ofrecerse como tu aplicación de correo predeterminada, para
que los enlaces de correo se abran aquí. Arrastra archivos a un mensaje para adjuntarlos, o pega
una imagen directamente. Cambia el asunto al responder o reenviar. Haz clic derecho mientras
escribes para cortar, copiar y pegar. Añade y edita contactos. Selecciona varios mensajes y
archívalos, bórralos o márcalos juntos. Define cada cuánto se repite un evento y cuándo termina.
Comparte un archivo desde otra aplicación. Elige el nombre con el que sale tu correo. Corregido:
los mensajes HTML vuelven a mostrar su diseño, el panel de lectura ya no parpadea y editar una
serie desde una repetición posterior mantiene las anteriores.
```

**Italiano**

```
Novità: Allodia Mail & Calendar può proporsi come app di posta predefinita, così i link e-mail si
aprono qui. Trascina i file su un messaggio per allegarli, o incollaci un'immagine direttamente.
Cambia l'oggetto quando rispondi o inoltri. Fai clic destro mentre scrivi per tagliare, copiare e
incollare. Aggiungi e modifica i contatti. Seleziona più messaggi e archiviali, eliminali o
contrassegnali insieme. Imposta ogni quanto si ripete un evento e quando finisce. Condividi un
file da un'altra app. Scegli il nome con cui parte la tua posta. Corretto: i messaggi HTML
mostrano di nuovo il proprio impaginato, il riquadro di lettura non lampeggia più e modificare una
serie da un'occorrenza successiva conserva le precedenti.
```

**Português**

```
Novo: o Allodia Mail & Calendar pode propor-se como a sua aplicação de e-mail predefinida, para
que as ligações de e-mail abram aqui. Arraste ficheiros para uma mensagem para os anexar, ou cole
aí uma imagem diretamente. Altere o assunto ao responder ou reencaminhar. Clique com o botão
direito enquanto escreve para cortar, copiar e colar. Adicione e edite contactos. Selecione várias
mensagens e arquive-as, elimine-as ou marque-as em conjunto. Defina de quanto em quanto tempo um
evento se repete e quando termina. Partilhe um ficheiro de outra aplicação. Escolha o nome com que
o seu correio é enviado. Corrigido: as mensagens HTML voltam a mostrar o seu grafismo, o painel de
leitura deixa de piscar e editar uma série a partir de uma ocorrência posterior mantém as
anteriores.
```

## android

Paste into: Google Play

Written, not assembled: 13 changes do not fit this store's cap. Every one of them is in the appendix below.

**English**

```
New: drag files in to attach them, paste a picture straight into a message, edit the subject when
you reply or forward, add and edit contacts, select several messages and act on the lot, set how
often an event repeats, share a file in from another app, and choose the name your mail goes out
under. Fixed: HTML mail keeps its own styling, the mailbox keeps your place, the reading pane no
longer flashes, and a series edit keeps its earlier occurrences.
```

**Nederlands**

```
Nieuw: bestanden erin slepen om bij te voegen, een afbeelding direct plakken, het onderwerp
aanpassen bij beantwoorden of doorsturen, contacten toevoegen en wijzigen, meerdere berichten
samen afhandelen, instellen hoe vaak een afspraak terugkeert, een bestand delen vanuit een andere
app, en de naam kiezen waaronder je e-mail uitgaat. Opgelost: HTML-mail houdt zijn opmaak, de
lijst onthoudt waar je was, het leesvenster flitst niet meer, en een reeks bewerken behoudt de
eerdere herhalingen.
```

**Deutsch**

```
Neu: Dateien zum Anhängen hineinziehen, ein Bild direkt einfügen, den Betreff beim Antworten oder
Weiterleiten ändern, Kontakte anlegen und bearbeiten, mehrere Nachrichten zusammen erledigen,
festlegen wie oft ein Termin sich wiederholt, eine Datei aus einer anderen App teilen, und den
Absendernamen wählen. Behoben: HTML-Mail behält ihre Gestaltung, die Liste merkt sich Ihre Stelle,
das Lesefenster blitzt nicht mehr, und eine Serienänderung behält die früheren Termine.
```

**Français**

```
Nouveau : glisser des fichiers pour les joindre, coller une image directement, modifier l'objet en
réponse ou transfert, ajouter et modifier des contacts, traiter plusieurs messages ensemble,
définir la fréquence d'un événement, partager un fichier depuis une autre app, et choisir le nom
sous lequel vos messages partent. Corrigé : le HTML garde sa mise en page, la liste retient votre
place, le volet de lecture ne clignote plus, et modifier une série conserve les occurrences
antérieures.
```

**Español**

```
Nuevo: arrastra archivos para adjuntarlos, pega una imagen directamente, cambia el asunto al
responder o reenviar, añade y edita contactos, gestiona varios mensajes a la vez, define cada
cuánto se repite un evento, comparte un archivo desde otra app y elige el nombre con el que sale
tu correo. Corregido: el HTML conserva su diseño, la lista recuerda dónde estabas, el panel de
lectura ya no parpadea y editar una serie mantiene las repeticiones anteriores.
```

**Italiano**

```
Novità: trascina i file per allegarli, incolla un'immagine direttamente, cambia l'oggetto quando
rispondi o inoltri, aggiungi e modifica i contatti, gestisci più messaggi insieme, imposta ogni
quanto si ripete un evento, condividi un file da un'altra app e scegli il nome con cui parte la
tua posta. Corretto: l'HTML mantiene il suo impaginato, la lista ricorda dove eri, il riquadro di
lettura non lampeggia più e modificare una serie conserva le occorrenze precedenti.
```

**Português**

```
Novo: arraste ficheiros para os anexar, cole uma imagem diretamente, altere o assunto ao responder
ou reencaminhar, adicione e edite contactos, trate várias mensagens em conjunto, defina de quanto
em quanto tempo um evento se repete, partilhe um ficheiro de outra aplicação e escolha o nome com
que o seu correio é enviado. Corrigido: o HTML mantém o seu grafismo, a lista guarda o seu lugar,
o painel de leitura deixa de piscar e editar uma série mantém as ocorrências anteriores.
```

## linux

Paste into: (no store yet)

**English**

```
Drag files onto a message you are writing to attach them. Drag a picture and you are asked whether
to show it in the message or send it as a file to download.

Change the subject when replying or forwarding. The field opens with the usual "Re:" or "Fwd:"
and is yours to edit before sending.

Paste a screenshot or a photo straight into a message and it appears where you are typing, sent
as part of the message rather than as a file to download.

Right-click while writing a message to cut, copy and paste, and to copy the address of a link in
the message you are replying to.

Add and edit contacts. Fill in a name, organisation, role, addresses and numbers, and save into any
address book your accounts can write to. Editing a person who is in two accounts asks which one you
mean, and changes nothing you did not touch.

Pick several messages and deal with them together: archive, trash, mark read or unread, flag.
Ctrl-click and Shift-click on a computer, Select or a long press on a phone.

Set how often an event repeats: daily, weekly, monthly or yearly, every few days or weeks, on the
weekdays you pick. Choose whether it ends on a date, after a number of times, or never.

Share a file to Allodia Mail & Calendar from any other app, and a new message opens with it
already attached.

Set the name your mail goes out under. You are asked once when an account is added, and can change
it any time in Settings, so your messages arrive as your name rather than just your address.

Editing a repeating event from a later occurrence no longer deletes the ones before it, and
changing the time there now moves the whole series by that much.

HTML messages show their own layout, fonts and inline images again, instead of plain unstyled text.

New-mail notifications keep arriving all session, instead of stopping after the first one.

Moving between messages no longer flashes a dark panel where the message should be; the page stays put and only the message on it changes.
```

**Nederlands**

```
Sleep bestanden op een bericht dat je schrijft om ze bij te voegen. Sleep je een afbeelding, dan
vragen we of je hem in het bericht wilt tonen of als bestand wilt meesturen.

Pas het onderwerp aan bij beantwoorden of doorsturen. Het veld begint met het gebruikelijke "Re:"
of "Fwd:" en je kunt het aanpassen voor je verzendt.

Plak een schermafbeelding of foto rechtstreeks in een bericht: hij verschijnt waar je typt en
gaat als onderdeel van het bericht mee, niet als bestand om te downloaden.

Klik met rechts terwijl je schrijft om te knippen, kopiëren en plakken, en om het adres te
kopiëren van een link in het bericht dat je beantwoordt.

Contacten toevoegen en wijzigen. Vul een naam, organisatie, functie, adressen en nummers in en sla
op in elk adresboek waar je accounts naartoe kunnen schrijven. Bij een contact in twee accounts
vraagt de app welke je bedoelt, en verandert er niets wat je niet hebt aangeraakt.

Kies meerdere berichten en handel ze samen af: archiveren, weggooien, als gelezen of ongelezen
markeren, markeren. Ctrl-klik en Shift-klik op een computer, Selecteren of lang indrukken op een
telefoon.

Stel in hoe vaak een afspraak zich herhaalt: dagelijks, wekelijks, maandelijks of jaarlijks, om de
zoveel dagen of weken, op de weekdagen die je kiest. Kies of hij stopt op een datum, na een aantal
keer, of nooit.

Deel een bestand vanuit een andere app met Allodia Mail & Calendar, en er opent een nieuw bericht
met het bestand er al aan.

Stel in onder welke naam je e-mail wordt verzonden. De app vraagt het eenmalig bij het toevoegen
van een account en je past het altijd aan in Instellingen, zodat berichten met je naam aankomen in
plaats van alleen je adres.

Een terugkerende afspraak bewerken vanaf een latere herhaling verwijdert de eerdere niet meer, en
de tijd daar aanpassen verschuift voortaan de hele reeks met datzelfde verschil.

HTML-berichten tonen weer hun eigen opmaak, lettertypen en ingesloten afbeeldingen, in plaats van kale tekst zonder opmaak.

Meldingen over nieuwe e-mail blijven de hele sessie komen in plaats van na de eerste te stoppen.

Als je van bericht wisselt zie je niet langer kort een donker vlak waar het bericht hoort te staan; de pagina blijft staan en alleen het bericht erop verandert.
```

**Deutsch**

```
Ziehen Sie Dateien auf eine Nachricht, die Sie schreiben, um sie anzuhängen. Bei einem Bild fragen
wir, ob es im Text erscheinen oder als Datei mitgehen soll.

Ändern Sie den Betreff beim Antworten oder Weiterleiten. Das Feld beginnt mit dem üblichen "Re:"
oder "Fwd:" und lässt sich vor dem Senden bearbeiten.

Fügen Sie ein Bildschirmfoto oder Foto direkt in eine Nachricht ein: Es erscheint dort, wo Sie
schreiben, und geht als Teil der Nachricht mit statt als Datei zum Herunterladen.

Klicken Sie beim Schreiben mit der rechten Maustaste, um auszuschneiden, zu kopieren und
einzufügen und um die Adresse eines Links in der beantworteten Nachricht zu kopieren.

Kontakte hinzufügen und bearbeiten. Tragen Sie Name, Organisation, Position, Adressen und Nummern
ein und sichern Sie in jedes Adressbuch, in das Ihre Konten schreiben dürfen. Bei einem Kontakt in
zwei Konten fragt die App, welches Sie meinen, und ändert nichts, was Sie nicht angefasst haben.

Wählen Sie mehrere Nachrichten aus und erledigen Sie sie zusammen: archivieren, löschen, als gelesen
oder ungelesen markieren, markieren. Strg-Klick und Umschalt-Klick am Computer, auf dem Telefon
Auswählen oder langes Drücken.

Legen Sie fest, wie oft sich ein Termin wiederholt: täglich, wöchentlich, monatlich oder jährlich,
alle paar Tage oder Wochen, an den von Ihnen gewählten Wochentagen. Wählen Sie, ob er an einem
Datum, nach einer Anzahl von Malen oder nie endet.

Teilen Sie eine Datei aus einer anderen App mit Allodia Mail & Calendar, und eine neue Nachricht
öffnet sich mit der Datei bereits im Anhang.

Legen Sie fest, unter welchem Namen Ihre E-Mails versendet werden. Die App fragt einmal beim
Hinzufügen eines Kontos danach, und Sie ändern ihn jederzeit in den Einstellungen, damit Ihre
Nachrichten mit Ihrem Namen statt nur Ihrer Adresse ankommen.

Wer einen Serientermin von einem späteren Termin aus bearbeitet, verliert die früheren nicht mehr,
und eine dort geänderte Uhrzeit verschiebt jetzt die ganze Serie um denselben Betrag.

HTML-Nachrichten zeigen wieder ihr eigenes Layout, ihre Schriften und eingebetteten Bilder statt reinem Text ohne Gestaltung.

Benachrichtigungen über neue E-Mails kommen die ganze Sitzung lang und hören nicht nach der ersten
auf.

Beim Wechsel zwischen Nachrichten blitzt keine dunkle Fläche mehr dort auf, wo die Nachricht stehen sollte; die Seite bleibt stehen und nur die Nachricht darauf wechselt.
```

**Français**

```
Faites glisser des fichiers sur un message en cours d'écriture pour les joindre. Pour une image,
nous demandons si elle doit apparaître dans le message ou partir en pièce jointe.

Modifiez l'objet lors d'une réponse ou d'un transfert. Le champ s'ouvre avec le « Re: » ou
« Fwd: » habituel et reste modifiable avant l'envoi.

Collez une capture d'écran ou une photo directement dans un message : elle apparaît là où vous
écrivez et part avec le message plutôt qu'en pièce jointe à télécharger.

Faites un clic droit pendant que vous écrivez pour couper, copier et coller, et pour copier
l'adresse d'un lien du message auquel vous répondez.

Ajoutez et modifiez des contacts. Saisissez un nom, une organisation, une fonction, des adresses et
des numéros, puis enregistrez dans n'importe quel carnet d'adresses où vos comptes peuvent écrire.
Pour un contact présent dans deux comptes, l'app demande lequel vous visez, et ne change rien
d'autre.

Sélectionnez plusieurs messages et traitez-les ensemble : archiver, mettre à la corbeille, marquer
comme lu ou non lu, signaler. Ctrl-clic et Maj-clic sur un ordinateur, Sélectionner ou appui long
sur un téléphone.

Définissez la fréquence de répétition d’un événement : chaque jour, semaine, mois ou année, tous
les quelques jours ou semaines, les jours que vous choisissez. Choisissez s’il se termine à une
date, après un certain nombre de fois, ou jamais.

Partagez un fichier depuis une autre application vers Allodia Mail & Calendar, et un nouveau
message s'ouvre avec le fichier déjà joint.

Choisissez le nom sous lequel vos messages partent. L'app vous le demande une fois lors de l'ajout
d'un compte et vous le modifiez quand vous voulez dans les Réglages, pour que vos messages arrivent
à votre nom plutôt qu'à votre seule adresse.

Modifier un événement récurrent depuis une occurrence ultérieure ne supprime plus les précédentes,
et y changer l’heure décale désormais toute la série d’autant.

Les messages HTML retrouvent leur mise en page, leurs polices et leurs images intégrées, au lieu d'un texte brut sans mise en forme.

Les notifications de nouveaux messages continuent d'arriver pendant toute la session au lieu de
s'arrêter après la première.

Passer d'un message à l'autre ne fait plus apparaître brièvement un fond sombre à la place du message ; la page reste en place et seul le message qui s'y trouve change.
```

**Español**

```
Arrastra archivos al mensaje que estás escribiendo para adjuntarlos. Con una imagen te
preguntamos si quieres mostrarla en el mensaje o enviarla como archivo.

Cambia el asunto al responder o reenviar. El campo se abre con el "Re:" o "Fwd:" habitual y
puedes editarlo antes de enviar.

Pega una captura de pantalla o una foto directamente en un mensaje: aparece donde estás
escribiendo y se envía dentro del mensaje, no como archivo para descargar.

Haz clic derecho mientras escribes para cortar, copiar y pegar, y para copiar la dirección de un
enlace del mensaje al que respondes.

Añade y edita contactos. Escribe un nombre, una organización, un cargo, direcciones y números, y
guarda en cualquier libreta de direcciones en la que tus cuentas puedan escribir. Si el contacto
está en dos cuentas, la app te pregunta a cuál te refieres y no cambia nada que no hayas tocado.

Seleccione varios mensajes y resuélvalos juntos: archivar, mover a la papelera, marcar como leído o
no leído, marcar. Ctrl-clic y Mayús-clic en un ordenador, Seleccionar o una pulsación larga en un
teléfono.

Define con qué frecuencia se repite un evento: cada día, semana, mes o año, cada pocos días o
semanas, en los días que elijas. Elige si termina en una fecha, después de un número de veces, o
nunca.

Comparte un archivo desde otra aplicación con Allodia Mail & Calendar y se abre un mensaje nuevo
con el archivo ya adjunto.

Elige el nombre con el que sale tu correo. La app te lo pregunta una vez al añadir una cuenta y
puedes cambiarlo cuando quieras en Ajustes, para que tus mensajes lleguen con tu nombre y no solo
con tu dirección.

Editar un evento periódico desde una repetición posterior ya no elimina las anteriores, y cambiar
la hora allí ahora desplaza toda la serie en la misma medida.

Los mensajes HTML vuelven a mostrar su propio diseño, sus tipografías y sus imágenes insertadas, en lugar de texto sin formato.

Los avisos de correo nuevo siguen llegando durante toda la sesión en lugar de detenerse tras el
primero.

Al pasar de un mensaje a otro ya no aparece un instante un panel oscuro donde debería estar el mensaje; la página se mantiene y solo cambia el mensaje que hay en ella.
```

**Italiano**

```
Trascina i file sul messaggio che stai scrivendo per allegarli. Per un'immagine ti chiediamo se
mostrarla nel messaggio o inviarla come file.

Cambia l'oggetto quando rispondi o inoltri. Il campo si apre con il solito "Re:" o "Fwd:" e resta
modificabile prima dell'invio.

Incolla uno screenshot o una foto direttamente in un messaggio: compare dove stai scrivendo e
viene inviata dentro il messaggio, non come file da scaricare.

Fai clic destro mentre scrivi per tagliare, copiare e incollare, e per copiare l'indirizzo di un
link nel messaggio a cui stai rispondendo.

Aggiungi e modifica i contatti. Inserisci nome, organizzazione, ruolo, indirizzi e numeri e salva in
qualsiasi rubrica su cui i tuoi account possono scrivere. Se il contatto è in due account, l'app
chiede quale intendi e non cambia nulla che tu non abbia toccato.

Seleziona più messaggi e gestiscili insieme: archivia, sposta nel cestino, segna come letto o non
letto, contrassegna. Ctrl-clic e Maiusc-clic su un computer, Seleziona o una pressione prolungata su
un telefono.

Imposta la frequenza con cui un evento si ripete: ogni giorno, settimana, mese o anno, ogni pochi
giorni o settimane, nei giorni che scegli. Scegli se termina in una data, dopo un certo numero di
volte, o mai.

Condividi un file da un'altra app con Allodia Mail & Calendar e si apre un nuovo messaggio con il
file già allegato.

Scegli il nome con cui parte la tua posta. L'app te lo chiede una volta quando aggiungi un account
e puoi cambiarlo quando vuoi nelle Impostazioni, così i messaggi arrivano con il tuo nome e non
solo con il tuo indirizzo.

Modificare un evento ricorrente da un'occorrenza successiva non elimina più quelle precedenti, e
cambiare l'ora lì ora sposta l'intera serie della stessa quantità.

I messaggi HTML mostrano di nuovo il proprio impaginato, i caratteri e le immagini incorporate, invece di testo senza formattazione.

Le notifiche di nuova posta continuano ad arrivare per tutta la sessione invece di fermarsi dopo
la prima.

Passando da un messaggio all'altro non compare più per un istante un riquadro scuro al posto del messaggio; la pagina resta ferma e cambia solo il messaggio su di essa.
```

**Português**

```
Arraste ficheiros para a mensagem que está a escrever para os anexar. No caso de uma imagem,
perguntamos se quer mostrá-la na mensagem ou enviá-la como ficheiro.

Altere o assunto ao responder ou reencaminhar. O campo abre com o habitual "Re:" ou "Fwd:" e pode
ser editado antes de enviar.

Cole uma captura de ecrã ou uma fotografia diretamente numa mensagem: aparece onde está a
escrever e segue dentro da mensagem, não como ficheiro para transferir.

Clique com o botão direito enquanto escreve para cortar, copiar e colar, e para copiar o endereço
de uma ligação da mensagem a que está a responder.

Adicione e edite contactos. Introduza um nome, uma organização, um cargo, endereços e números e
guarde em qualquer livro de endereços onde as suas contas possam escrever. Num contacto que esteja
em duas contas, a aplicação pergunta qual pretende e não altera nada em que não tenha tocado.

Selecione várias mensagens e trate-as em conjunto: arquivar, mover para o lixo, marcar como lida ou
não lida, sinalizar. Ctrl-clique e Shift-clique num computador, Selecionar ou toque longo num
telemóvel.

Defina com que frequência um evento se repete: todos os dias, semanas, meses ou anos, a cada poucos
dias ou semanas, nos dias que escolher. Escolha se termina numa data, após um número de vezes, ou
nunca.

Partilhe um ficheiro de outra aplicação com o Allodia Mail & Calendar e abre-se uma mensagem nova
com o ficheiro já anexado.

Defina o nome com que o seu correio é enviado. A aplicação pergunta uma vez ao adicionar uma conta
e pode alterá-lo quando quiser nas Definições, para que as mensagens cheguem com o seu nome e não
apenas com o seu endereço.

Editar um evento recorrente a partir de uma ocorrência posterior deixa de apagar as anteriores, e
alterar a hora aí passa a deslocar toda a série na mesma medida.

As mensagens HTML voltam a mostrar o seu próprio grafismo, tipos de letra e imagens incorporadas, em vez de texto sem qualquer formatação.

As notificações de correio novo continuam a chegar durante toda a sessão em vez de pararem depois
da primeira.

Ao mudar de mensagem deixa de surgir por instantes uma área escura no lugar da mensagem; a página mantém-se e muda apenas a mensagem que está nela.
```

## Changes in this release

Engineering commentary, carried over from each fragment. Never pasted into a store.

### Offer to become your default mail app — `ask-to-be-your-default-mail-app` (macos, windows, minor)

> The offer is put **once**, and the core decides when: never before an account exists, never when
> the app is already the handler, and never twice. A dismissed prompt counts as answered, because
> an unanswered question is not permission to ask again, and Settings → General is the way back.
> No client keeps its own "have we asked?" flag, so nothing can disagree with what Settings shows.
>
> What the platforms can actually do differs more than expected. macOS shows a system consent
> alert, but only in the Developer ID build: the App Sandbox refuses `setDefaultApplication`, so
> the Mac App Store build reports `unsupported` at runtime (read from the sandbox container
> variable) and shows neither the offer nor the row. Windows has had no API to set a handler since
> Windows 10 by design, so it deep-links its own Default apps page. iOS needs the
> `com.apple.developer.mail-client` entitlement, which Apple grants by request; until then it
> reports `unsupported` too, because sending someone to a list this app is not in is worse than
> silence. Linux and Android can be asked nothing at all.

### Drag files onto a message you are writing — `composer-drag-files-in` (macos, ios, windows, android, linux, minor)

> The page refuses the drop and the host takes it: web code only ever sees a `File` with no path,
> which can neither be streamed from disk on send nor listed for removal. A picture is the one case
> with two sensible answers, so the client asks in the reader's terms rather than choosing for
> them; the "show it" answer reuses the paste path, so the two cannot behave differently.

### Rename a thread when you reply or forward — `composer-editable-subject` (macos, ios, windows, android, linux, minor)

> The field opens with the subject the core would have sent, derived by the core rather than by
> each client: it is now what goes on the wire, so a client-side `"Re: " + subject` would differ on
> a reply to a reply. An empty field is honoured rather than refilled; only a caller with no
> subject field at all (the MCP surface) still gets the derived one.

### Paste a picture straight into a message — `composer-paste-a-picture` (macos, ios, windows, android, linux, minor)

> The bytes travel inside the editor document as a `data:` URI and the shared core turns them into
> a `cid:` `multipart/related` part on send, which is the same destination a signature's logo
> already reaches. Doing it that way keeps the whole path in the shared bundle and shared Rust, so
> the remaining three clients inherit it without a host bridge each. Deliberately left out: a
> toolbar button that browses for a picture, and any resizing of what was pasted.

### A right-click menu while you write — `composer-right-click-menu` (macos, windows, android, linux, minor)

> Each host filters its WebView's own menu rather than drawing one, so every surviving item keeps
> the platform's label, keyboard equivalent and behaviour. What is removed is what a composer must
> not offer: opening a link (navigation is blocked, so the item did nothing), downloading one,
> reloading the document, and the web inspector.

### Adding and editing contacts — `contact-editing` (macos, ios, windows, android, linux, minor)

> Contacts have been readable since 0.6; this makes them writable. The shape follows from the one
> thing the list already does: a row is a **person**, which the engine assembled from the cards
> several accounts hold. A write cannot be a person, so an edit names a card, the client asks which
> account when there is more than one, and the form is seeded from that card rather than from the
> merged detail on screen. The create destination is a writable address book and nothing else, so a
> user with none is offered no create at all rather than a save that fails on the server after they
> have typed everything in. A patch carries only the fields that changed, so an edited phone number
> cannot quietly strip an address's work label, an organisation's departments, or a postal address
> and photo the form never showed. Deleting a contact is deliberately not here: it is the one
> contacts write nobody can undo, and it wants its own confirmation.

### Mail links open the composer on Mac — `mail-links-open-the-composer-on-mac` (macos, minor)

> Gate 12's last platform that can actually ship it. Nothing new was decided: the URI is decoded by
> `parse_mailto_uri`, so Apple inherits the same header allowlist the other three already honour,
> and `MailLinkRequest` adds only an id, for the reason `AgentDraftRequest` carries one. Without it
> `.fullScreenCover(item:)` compares the second tap of the same link equal to the first and silently
> does nothing.
>
> `onOpenURL` sits on the shell rather than the `WindowGroup` because the model is that view's own
> state. No scheme gate is needed, unlike the other three clients: every OAuth redirect is captured
> inside its own `ASWebAuthenticationSession` and never arrives as an opened URL.
>
> A link arriving before the first account is held on the model and opened once one exists, and one
> arriving over a draft goes through the discard guard, so a web page cannot throw away a
> half-written message.
>
> ⚠️ **iPhone and iPad are deliberately not claimed.** The same code and the same
> `CFBundleURLTypes` declaration ship there, but iOS routes a mail link to the app set as the
> *default* mail app and to nothing else, and being set as that needs
> `com.apple.developer.mail-client`, which Apple grants only by request. So `onOpenURL` never fires
> for a mail link on iOS yet, the capability matrix says 🚧, and this note says Mac alone. See
> `docs/os-integration.md` → Known gaps.

### Scroll the calendar straight past the weekend — `scroll-the-calendar-straight-past-the-weekend` (macos, ios, minor)

> The Mac and the phone stop paging the calendar by the week and draw one continuous strip of days
> instead, the model Windows already ships and Apple's own Calendar uses on both. The weeks are laid
> end to end with the hour ruler pinned beside them, so a grid resting on Sunday-and-Monday is a
> coherent frame rather than half of each of two pages, and the strip comes to rest on a **day** at
> every zoom and for every input. That needs no threshold and no judgement about whether a drag
> travelled far enough, which is the machinery that rubber-bands a slow pan back home.
> `CalendarStrip` is a plain Swift value type, so where the grid rests, which weeks it is showing and
> what a pan does to it are pinned without a viewport. Deliberately left out: the iPad's trackpad is
> wired but unverified, and the frame budget has not been measured on a release build. Both are under
> `docs/calendar.md` → "Known gaps".

### Select several messages and act on the lot — `select-several-messages` (macos, ios, windows, android, linux, minor)

> The selection stays in each client's own list control, so the modifier, focus and
> assistive-technology behaviour people expect is the platform's rather than ours. What is shared is
> the write: one intent applies the action to every selected row, hides them together and syncs each
> account once, where a loop over the single-row intents would sync it once per row. A conversation
> row stands for its whole thread and the core expands it, on the same rule the thread archive
> already followed: a copy filed in Sent never leaves Sent. Where there is a reading pane the bar of
> actions is standing chrome across both panes, disabled until something is picked, so opening a
> selection moves neither the rows nor the message being read; the count is stated in the pane,
> above one row only, since one row is the message the pane is already showing. Dragging rows into a folder is not
> part of this and has no core action yet; `docs/list-selection.md` carries that gap.

### Set how often an event repeats — `set-how-often-an-event-repeats` (macos, ios, windows, android, linux, minor)

> Four controls (a frequency, how many periods to skip, which weekdays, and what ends it) over a
> draft the core hands out and takes back. Which rules the editor may open, what a save should send,
> and which parts of a rule survive an edit that never touched them are one decision in
> `mailcal-account`, not five in the clients: a monthly series pinned to the month's last day keeps
> that when the user only moves its end date, because the client never rebuilds the rule itself.
> A rule the core could not put into a sentence is shown and not offered, which is the same
> judgement `Simple` against `Complex` already makes: an editor seeded from a partial picture
> would write the rest away. Deliberately left out: a control for the monthly and yearly anchor
> ("the second Monday"), which is carried but not editable, and a way to stop a repeat too detailed
> to state. Both are under `docs/calendar.md` → "Known gaps".

### Share a file into a new message — `share-a-file-into-a-new-message` (windows, android, linux, minor)

> The shared half is `mailcal_composer::share`, so each client inherits the naming, the typing,
> the cap and the refusal reporting rather than deciding again. Linux went first because it is the
> one client a Linux workstation can actually run, which is where the design got its first real
> exercise; Android and Windows followed, each gating its own half (the intent, the staged name)
> in a suite that needs no device. Apple is still to come: it needs a Share Extension target.
>
> Windows stages the shared bytes before its `ShareOperation` reports complete, because that is
> when its access to them ends; a path taken straight from one would be unreadable by the time the
> user pressed Send.
>
> Linux has no share portal, so "Open With" plus a local `--attach` is what a desktop actually
> offers. That has a cost worth stating: a `MimeType=` entry reads as "this app opens that type",
> and there is no key for "attaches but does not display", so the list is kept to what a person
> plausibly emails and a test pins it exactly.
>
> `mailto:?attach=` stays unhonoured, and now says so as a standing rule: a handler cannot tell a
> URI that came from `xdg-email` from one that came from a web page.

### The name your mail goes out under — `the-name-your-mail-goes-out-under` (macos, ios, windows, android, linux, minor)

> Mail from an IMAP account left as a bare address, because nothing ever asked what to call the
> sender. The name is now the core's: one value per account, stored with the other per-account
> preferences and put in the `From` of every send. It is asked for once the account connects
> rather than on the first screen, which stays the address field and nothing else, and it is
> filled in already where the provider knows it. Three providers keep a name of their own and
> disagree about who owns it, so the engine reports which of the three cases an account is in: no
> server-side name at all, one the account holder can change, or one an organisation's directory
> hands down. Settings offers a field for the first two and states the third rather than showing
> an editor nobody can use. The value is sanitised on store, so a name pasted out of a document
> cannot smuggle a second header into a message. The composer's From then shows what the recipient
> will see, `Name <address>`, from one exported formatter rather than four hand-rolled ones: the
> case worth getting right is the empty one, where the label is the address alone and never a lone
> pair of angle brackets. The sidebar still shows the address, which is what an account is
> recognised by. No second store note: this is the same feature reaching the place a sender picks
> who a message comes from, and the one above already promises it.

### A dismissed event editor stays dismissed — `a-dismissed-event-editor-stays-dismissed` (macos, ios, patch)

> Cancel and Save both closed the event editor and both immediately reopened it, on iPhone and
> iPad. `.sheet(item:)` keeps its content alive while the sheet animates away and that content is
> still writing (a text field commits, a focus state tears down), and the binding it was given read
> through to the value the sheet had been presented with. So a late write handed that value back to
> storage, the item was non-`nil` again, and the sheet presented itself a second time. The read
> still falls back, because SwiftUI evaluates the content closure on the frame where the item has
> just been cleared and force-unwrapping there traps; the write no longer does.
> `sheetItemBinding` holds both halves, and `SheetItemBindingTests` drives them without a sheet,
> since there is no Apple UI-test target to present one.

### A series edit keeps the occurrences before the one you opened — `a-series-edit-keeps-its-earlier-occurrences` (macos, ios, windows, android, linux, patch)

> Editing a repeating event from one of its later occurrences and answering *All events* wrote that
> occurrence's clocks onto the series, so its start moved forward and every occurrence before it
> stopped existing. It needed no time change to happen: a rename was enough, and the repeat editor
> made it easy to reach because a rule change means the series without asking. An edit meant for
> the series now carries the *shift* the user made, applied to the series' own clock, which is what
> a drag on a series already does; `EventEdit::times_from_occurrence` names where the clocks were
> read from, and `calendar_series_shift_tests.rs` pins the four cases.

### The mailbox keeps your place — `android-mail-list-keeps-your-place` (android, patch)

> The list's scroll position and the search chrome were both `remember`ed inside MailboxScreen, and
> opening a message replaces that screen rather than covering it, so both died with the composition:
> every message read from halfway down the inbox cost the user their place, and a message opened
> from search results dropped them back into a list still narrowed by a query with nothing on screen
> saying so. Both now live on the activity. The search half also matches a lifetime: the core clears
> its query on nothing but this client asking, so the chrome has to last exactly as long.

### HTML mail renders with its own styling again — `html-mail-renders-with-its-own-styling-again` (macos, ios, windows, android, linux, patch)

> Every HTML message rendered unstyled, on every client, and every inline image was blocked with
> it: the base stylesheet, the message's `<style>` block and its inline styles all had no effect,
> so mail came out as browser-default serif on a bare page. The reading document's
> `Content-Security-Policy` separated two of its directives with a comma, and a `content`
> attribute is a policy *list*, so that started a second policy enforced alongside the first. The
> document got the intersection of the two, which put `default-src 'none'` in one policy and the
> `style-src 'unsafe-inline'` and `img-src data:` meant to soften it in the other, and both fell
> back to `'none'`. The separator is now a semicolon, and
> `the_document_carries_one_policy_rather_than_two` asserts on the separators, which is what the
> existing per-directive assertions structurally could not see.

### New-mail notifications keep arriving — `new-mail-notifications-keep-arriving` (linux, patch)

> `ashpd` keeps one session-bus connection for the whole process, and zbus drives it from the
> runtime that opened it. Both the secure store (`oo7` asks the Secret portal for the keyring key
> inside a sandbox) and `notifications::post` built a runtime of their own, so the first one to
> finish took the shared connection's reader with it and every later portal call awaited a reply
> that could never arrive: no error, no timeout, a thread parked for the life of the process. The
> new-mail scan is held open across that call, so the wedge stopped the client noticing mail at
> all, not merely notifying about it. One process-global runtime now serves every portal caller
> (`host_runtime`), and the post is bounded so a portal that stops answering costs one pass rather
> than the session. The suite's disabled-notifications leg asserts an absence and so passed
> silently throughout; it now runs the enabled half first and measures the silence against it.

### The reading pane no longer flashes between messages — `the-reading-pane-no-longer-flashes-between-messages` (macos, ios, windows, android, linux, patch)

> Two causes, both showing as black-to-white against a dark theme. The body area is the page a
> message is drawn on, and every client left it transparent until a body arrived, which punched a
> hole in that page for the length of the open (75–82 ms, measured). `MESSAGE_CANVAS` now sits
> beside the base stylesheet and is interpolated into it, crosses the FFI as `message_canvas`, and
> every client paints it behind the whole body area in a light appearance. A web view needs one
> thing more: WebKitGTK presents a black frame until the document's first paint (467 ms on a heavy
> message at full screen), so the Linux pane holds the canvas until the load reports finished.
