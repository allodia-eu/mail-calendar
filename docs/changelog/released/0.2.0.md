# 0.2.0 — 2026-07-20

## macos, ios, windows, android

Paste into: Microsoft Store · App Store Connect (macOS) · App Store Connect (iOS/iPadOS) · Google Play

**English**

```
Initial release of Allodia Mail & Calendar.
```

**Nederlands**

```
Eerste release van Allodia Mail & Calendar.
```

## Changes in this release

Engineering commentary, carried over from each fragment. Never pasted into a store.

### First submission to the three stores — `first-submission` (macos, ios, windows, android, minor)

> Per store convention the "What's new" field is **left blank** for a first release; the note above
> is the fallback to use if a store requires a non-empty value (and is the canonical text once the
> second release supersedes it).
>
> **This release predates the seven-language catalog**, so it carries `en` + `nl` only. A note is
> written to be *pasted at release*: a version already submitted in the old locale set is history
> and is left as it was shipped, rather than back-filled with five translations nobody pasted.
