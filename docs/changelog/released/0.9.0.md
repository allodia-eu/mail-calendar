# 0.9.0 — 2026-09-13

## macos

Paste into: App Store Connect (macOS)

**English**

```
Leave a search on macOS with the clear button in the search field.

Search is now centred at the top of the window on Mac, Windows and Linux, and New Mail has moved up there with it. Sync sits beside Archive and Delete, and on iPhone and iPad you pull the list down to sync. Your accounts' inboxes are now one "All Accounts" group you can open and shut.

A newsletter written for a wider screen now reflows to fit the reading pane instead of running off the right edge, at whatever width you read it.

An open message can now be saved as an .eml file, from the new menu at the end of the reading toolbar.

Search results are now grouped into conversations when conversation grouping is on.

You can now pinch to zoom a message, and every message opens at its own scale again.

A list or a selection holding one thing now reads "1 conversation" rather than "1 conversations".

Forwarding a message now brings its attachments into the composer, where you can remove any you would rather not pass on.

A folder holding one unread message now reads as a singular in French.

Mail accounts on JMAP providers that hand setup to a separate server, such as Thundermail, can now be added.

Mail arriving in a folder you opened yourself now shows up on a refresh, and message previews appear as soon as the bodies are downloaded.

Newsletters that adapt to a small screen now do so in the reading view, instead of being cut off at the right edge on a phone.

Search results no longer flicker between the right answer and an unrelated list while you are still typing.

Subjects and sender names in alphabets such as Japanese now read correctly; mail synced earlier corrects itself on the next full sync.
```

**Nederlands**

```
Verlaat een zoekopdracht op macOS met de wisknop in het zoekveld.

Zoeken staat nu gecentreerd bovenaan het venster op de Mac, Windows en Linux, en Nieuwe e-mail is meeverhuisd naar boven. Synchroniseren staat naast Archiveren en Verwijderen, en op de iPhone en iPad trek je de lijst naar beneden om te synchroniseren. De postvakken IN van je accounts vormen nu één groep "Alle accounts" die je kunt openen en sluiten.

Een nieuwsbrief die voor een breder scherm is gemaakt, past zich nu aan het leesvenster aan in plaats van weg te vallen aan de rechterkant, op elke breedte waarop u leest.

Een geopend bericht kan nu worden opgeslagen als .eml-bestand, via het nieuwe menu aan het eind van de leesbalk.

Zoekresultaten worden nu gegroepeerd in gesprekken wanneer groeperen op gesprek aanstaat.

U kunt nu met een knijpbeweging in- en uitzoomen op een bericht, en elk bericht opent weer op zijn eigen schaal.

Een lijst of selectie met één item toont nu "1 gesprek" in plaats van "1 gesprekken".

Bij het doorsturen van een bericht staan de bijlagen nu meteen in het opstelvenster, waar u kunt weglaten wat u liever niet meestuurt.

Een map met één ongelezen bericht gebruikt nu de juiste enkelvoudsvorm in het Frans.

E-mailaccounts bij JMAP-aanbieders die het instellen doorverwijzen naar een aparte server, zoals Thundermail, kunnen nu worden toegevoegd.

Post die binnenkomt in een map die je zelf hebt geopend, verschijnt nu bij een vernieuwing, en berichtvoorbeelden verschijnen zodra de berichten zijn opgehaald.

Nieuwsbrieven die zich aanpassen aan een klein scherm doen dat nu ook in de leesweergave, in plaats van op een telefoon rechts weg te vallen.

Zoekresultaten wisselen niet langer tussen het juiste antwoord en een lijst die er niets mee te maken heeft terwijl u nog typt.

Onderwerpen en afzendernamen in bijvoorbeeld het Japans worden nu correct weergegeven; eerder opgehaalde berichten herstellen zich bij de volgende volledige synchronisatie.
```

**Deutsch**

```
Verlassen Sie eine Suche unter macOS über die Schaltfläche zum Zurücksetzen im Suchfeld.

Die Suche steht jetzt zentriert am oberen Fensterrand auf dem Mac, unter Windows und Linux, und Neue E-Mail ist mit nach oben gewandert. Synchronisieren sitzt neben Archivieren und Löschen, und auf iPhone und iPad ziehen Sie die Liste nach unten, um zu synchronisieren. Die Posteingänge Ihrer Konten bilden jetzt eine Gruppe "Alle Konten", die Sie öffnen und schließen können.

Ein Newsletter, der für einen breiteren Bildschirm gemacht wurde, passt sich jetzt dem Lesebereich an, statt am rechten Rand abgeschnitten zu werden, und das bei jeder Lesebreite.

Eine geöffnete Nachricht lässt sich jetzt als .eml-Datei sichern, über das neue Menü am Ende der Leseleiste.

Suchergebnisse werden jetzt zu Konversationen gruppiert, wenn die Konversationsgruppierung aktiv ist.

Sie können eine Nachricht jetzt per Fingergeste zoomen, und jede Nachricht öffnet wieder in ihrem eigenen Maßstab.

Eine Liste oder Auswahl mit einem Element zeigt jetzt "1 Unterhaltung" statt "1 Unterhaltungen".

Beim Weiterleiten einer Nachricht stehen ihre Anhänge jetzt im Editor, wo Sie entfernen können, was Sie nicht mitsenden möchten.

Ein Ordner mit einer ungelesenen Nachricht verwendet im Französischen jetzt die richtige Einzahlform.

E-Mail-Konten bei JMAP-Anbietern, die die Einrichtung an einen separaten Server weiterleiten, etwa Thundermail, lassen sich jetzt hinzufügen.

Post, die in einem selbst geöffneten Ordner eingeht, erscheint jetzt beim Aktualisieren, und die Nachrichtenvorschau erscheint, sobald die Nachrichten geladen sind.

Newsletter, die sich an kleine Bildschirme anpassen, tun dies jetzt auch in der Leseansicht, statt auf dem Telefon am rechten Rand abgeschnitten zu werden.

Suchergebnisse springen nicht mehr zwischen dem richtigen Treffer und einer unpassenden Liste hin und her, während Sie noch tippen.

Betreffzeilen und Absendernamen etwa in japanischer Schrift werden jetzt korrekt angezeigt; zuvor abgerufene Nachrichten korrigieren sich bei der nächsten vollständigen Synchronisierung.
```

**Français**

```
Quittez une recherche sur macOS grâce au bouton d'effacement du champ de recherche.

La recherche est désormais centrée en haut de la fenêtre sur Mac, Windows et Linux, et Nouveau message l'y a rejointe. Synchroniser se trouve à côté d'Archiver et de Supprimer, et sur iPhone et iPad vous tirez la liste vers le bas pour synchroniser. Les boîtes de réception de vos comptes forment désormais un groupe « Tous les comptes » que vous pouvez ouvrir et fermer.

Une lettre d'information conçue pour un écran plus large s'adapte désormais au volet de lecture au lieu de déborder à droite, quelle que soit la largeur à laquelle vous la lisez.

Un message ouvert peut désormais être enregistré au format .eml, depuis le nouveau menu à la fin de la barre de lecture.

Les résultats de recherche sont désormais regroupés en conversations lorsque le regroupement par conversation est activé.

Vous pouvez désormais zoomer sur un message par pincement, et chaque message s'ouvre de nouveau à sa propre échelle.

Une liste ou une sélection ne contenant qu'un élément affiche désormais "1 conversation" et non "1 conversations".

Le transfert d'un message place désormais ses pièces jointes dans la fenêtre de rédaction, où vous pouvez retirer celles que vous préférez ne pas transmettre.

Un dossier ne contenant qu'un message non lu affiche désormais « 1 non lu » et non « 1 non lus ».

Les comptes de messagerie chez les fournisseurs JMAP qui confient la configuration à un serveur distinct, comme Thundermail, peuvent désormais être ajoutés.

Le courrier qui arrive dans un dossier que vous avez ouvert vous-même apparaît désormais lors d'une actualisation, et les aperçus de messages s'affichent dès que les messages sont téléchargés.

Les lettres d'information qui s'adaptent aux petits écrans le font désormais dans la vue de lecture, au lieu d'être coupées à droite sur un téléphone.

Les résultats de recherche n'alternent plus entre la bonne réponse et une liste sans rapport pendant que vous tapez.

Les objets et les noms d'expéditeurs écrits par exemple en japonais s'affichent désormais correctement ; les messages déjà récupérés se corrigent à la prochaine synchronisation complète.
```

**Español**

```
Salga de una búsqueda en macOS con el botón de borrado del campo de búsqueda.

La búsqueda aparece ahora centrada en la parte superior de la ventana en Mac, Windows y Linux, y Nuevo correo se ha trasladado allí con ella. Sincronizar está junto a Archivar y Eliminar, y en el iPhone y el iPad tiras de la lista hacia abajo para sincronizar. Las bandejas de entrada de tus cuentas forman ahora un grupo "Todas las cuentas" que puedes abrir y cerrar.

Un boletín pensado para una pantalla más ancha ahora se adapta al panel de lectura en lugar de salirse por la derecha, sea cual sea el ancho con el que lo lea.

Un mensaje abierto ya se puede guardar como archivo .eml, desde el nuevo menú al final de la barra de lectura.

Los resultados de búsqueda ahora se agrupan en conversaciones cuando la agrupación por conversación está activada.

Ahora puede acercar o alejar un mensaje pellizcando, y cada mensaje vuelve a abrirse a su propia escala.

Una lista o selección con un solo elemento ahora muestra "1 conversación" en lugar de "1 conversaciones".

Al reenviar un mensaje, sus archivos adjuntos aparecen ahora en el editor, donde puede quitar los que prefiera no enviar.

Una carpeta con un solo mensaje sin leer ahora usa el singular correcto en francés.

Las cuentas de correo de proveedores JMAP que dirigen la configuración a un servidor distinto, como Thundermail, ya se pueden añadir.

El correo que llega a una carpeta que has abierto tú aparece ahora al actualizar, y las vistas previas de los mensajes se muestran en cuanto se descargan.

Los boletines que se adaptan a una pantalla pequeña ahora lo hacen en la vista de lectura, en lugar de quedar cortados por la derecha en el teléfono.

Los resultados de búsqueda ya no alternan entre la respuesta correcta y una lista sin relación mientras sigue escribiendo.

Los asuntos y los nombres de remitente escritos, por ejemplo, en japonés ya se muestran correctamente; los mensajes ya descargados se corrigen en la próxima sincronización completa.
```

**Italiano**

```
Uscite da una ricerca su macOS con il pulsante di cancellazione nel campo di ricerca.

La ricerca è ora centrata in cima alla finestra su Mac, Windows e Linux, e Nuovo messaggio si è spostato lassù con lei. Sincronizza si trova accanto ad Archivia ed Elimina, e su iPhone e iPad trascini l'elenco verso il basso per sincronizzare. Le caselle di posta in arrivo dei tuoi account formano ora un gruppo "Tutti gli account" che puoi aprire e chiudere.

Una newsletter pensata per uno schermo più largo ora si adatta al riquadro di lettura invece di uscire dal bordo destro, a qualunque larghezza la si legga.

Un messaggio aperto ora può essere salvato come file .eml, dal nuovo menu alla fine della barra di lettura.

I risultati di ricerca ora sono raggruppati in conversazioni quando il raggruppamento per conversazione è attivo.

Ora si può ingrandire un messaggio con un pizzico, e ogni messaggio torna ad aprirsi alla propria scala.

Un elenco o una selezione con un solo elemento ora mostra "1 conversazione" anziché "1 conversazioni".

Inoltrando un messaggio, i suoi allegati compaiono ora nell'editor, dove è possibile rimuovere quelli che si preferisce non inviare.

Una cartella con un solo messaggio da leggere ora usa il singolare corretto in francese.

Gli account di posta su provider JMAP che affidano la configurazione a un server separato, come Thundermail, ora possono essere aggiunti.

La posta che arriva in una cartella aperta da te ora compare con un aggiornamento e le anteprime dei messaggi appaiono appena i messaggi sono scaricati.

Le newsletter che si adattano agli schermi piccoli ora lo fanno anche nella vista di lettura, invece di essere tagliate a destra sul telefono.

I risultati di ricerca non oscillano più fra la risposta giusta e un elenco che non c'entra mentre state ancora digitando.

Gli oggetti e i nomi dei mittenti scritti ad esempio in giapponese ora vengono visualizzati correttamente; i messaggi già scaricati si correggono alla successiva sincronizzazione completa.
```

**Português**

```
Saia de uma pesquisa no macOS com o botão de limpar do campo de pesquisa.

A pesquisa aparece agora centrada no topo da janela no Mac, no Windows e no Linux, e Nova mensagem subiu para lá com ela. Sincronizar fica junto de Arquivar e Eliminar, e no iPhone e no iPad arrasta a lista para baixo para sincronizar. As caixas de entrada das suas contas formam agora um grupo "Todas as contas" que pode abrir e fechar.

Uma newsletter feita para um ecrã mais largo passa a adaptar-se ao painel de leitura em vez de sair pela margem direita, seja qual for a largura a que a lê.

Uma mensagem aberta já pode ser guardada como ficheiro .eml, a partir do novo menu no fim da barra de leitura.

Os resultados da pesquisa passam a ser agrupados em conversas quando o agrupamento por conversa está ativo.

Agora pode ampliar uma mensagem com dois dedos, e cada mensagem volta a abrir à sua própria escala.

Uma lista ou seleção com um só item passa a mostrar "1 conversa" em vez de "1 conversas".

Ao reencaminhar uma mensagem, os seus anexos passam a aparecer no editor, onde pode remover os que prefira não enviar.

Uma pasta com uma só mensagem por ler passa a usar o singular correto em francês.

As contas de correio em fornecedores JMAP que encaminham a configuração para um servidor separado, como o Thundermail, já podem ser adicionadas.

O correio que chega a uma pasta que abriu aparece agora ao atualizar, e as pré-visualizações das mensagens surgem assim que as mensagens são transferidas.

As newsletters que se adaptam a ecrãs pequenos passam a fazê-lo na vista de leitura, em vez de ficarem cortadas à direita no telemóvel.

Os resultados da pesquisa deixam de alternar entre a resposta certa e uma lista sem relação enquanto continua a escrever.

Os assuntos e os nomes dos remetentes escritos, por exemplo, em japonês passam a ser apresentados corretamente; as mensagens já descarregadas corrigem-se na próxima sincronização completa.
```

## ios

Paste into: App Store Connect (iOS/iPadOS)

**English**

```
On iPhone and iPad you now pull the list down to sync. Your accounts' inboxes are now one "All Accounts" group you can open and shut.

A newsletter written for a wider screen now reflows to fit the reading pane instead of running off the right edge, at whatever width you read it.

New-mail notifications now show how the message begins, after the sender and the subject.

An open message can now be saved as an .eml file, from the new menu at the end of the reading toolbar.

Search results are now grouped into conversations when conversation grouping is on.

On iPhone and iPad the sender, recipients and attachments now scroll away with the message, leaving reply, archive and delete in place at the top.

You can now pinch to zoom a message, and every message opens at its own scale again.

A list or a selection holding one thing now reads "1 conversation" rather than "1 conversations".

Forwarding a message now brings its attachments into the composer, where you can remove any you would rather not pass on.

A folder holding one unread message now reads as a singular in French.

Mail accounts on JMAP providers that hand setup to a separate server, such as Thundermail, can now be added.

Pulling down to sync on the mailbox list no longer hides the search field or jolts the messages, and the list's menu is gone: both of its options are in Settings.

Pinching the calendar on iPhone and iPad zooms it again, both the hours and the days.

An open message now fits the screen on iPhone: the date, the actions and the message itself no longer run off the edges.

Mail arriving in a folder you opened yourself now shows up on a refresh, and message previews appear as soon as the bodies are downloaded.

Newsletters that adapt to a small screen now do so in the reading view, instead of being cut off at the right edge on a phone.

Search results no longer flicker between the right answer and an unrelated list while you are still typing.

Subjects and sender names in alphabets such as Japanese now read correctly; mail synced earlier corrects itself on the next full sync.
```

**Nederlands**

```
Op de iPhone en iPad trek je de lijst nu naar beneden om te synchroniseren. De postvakken IN van je accounts vormen nu één groep "Alle accounts" die je kunt openen en sluiten.

Een nieuwsbrief die voor een breder scherm is gemaakt, past zich nu aan het leesvenster aan in plaats van weg te vallen aan de rechterkant, op elke breedte waarop u leest.

Meldingen van nieuwe e-mail tonen nu ook hoe het bericht begint, na de afzender en het onderwerp.

Een geopend bericht kan nu worden opgeslagen als .eml-bestand, via het nieuwe menu aan het eind van de leesbalk.

Zoekresultaten worden nu gegroepeerd in gesprekken wanneer groeperen op gesprek aanstaat.

Op de iPhone en iPad schuiven de afzender, de geadresseerden en de bijlagen nu mee met het bericht, terwijl beantwoorden, archiveren en verwijderen bovenaan blijven staan.

U kunt nu met een knijpbeweging in- en uitzoomen op een bericht, en elk bericht opent weer op zijn eigen schaal.

Een lijst of selectie met één item toont nu "1 gesprek" in plaats van "1 gesprekken".

Bij het doorsturen van een bericht staan de bijlagen nu meteen in het opstelvenster, waar u kunt weglaten wat u liever niet meestuurt.

Een map met één ongelezen bericht gebruikt nu de juiste enkelvoudsvorm in het Frans.

E-mailaccounts bij JMAP-aanbieders die het instellen doorverwijzen naar een aparte server, zoals Thundermail, kunnen nu worden toegevoegd.

Naar beneden trekken om te synchroniseren verbergt het zoekveld niet meer en laat de berichten niet meer verspringen, en het menu van de lijst is weg: beide opties staan in Instellingen.

Knijpen in de agenda op iPhone en iPad zoomt weer in en uit, zowel op de uren als op de dagen.

Een geopend bericht past nu op het scherm op de iPhone: de datum, de acties en het bericht zelf lopen niet meer van de randen af.

Post die binnenkomt in een map die je zelf hebt geopend, verschijnt nu bij een vernieuwing, en berichtvoorbeelden verschijnen zodra de berichten zijn opgehaald.

Nieuwsbrieven die zich aanpassen aan een klein scherm doen dat nu ook in de leesweergave, in plaats van op een telefoon rechts weg te vallen.

Zoekresultaten wisselen niet langer tussen het juiste antwoord en een lijst die er niets mee te maken heeft terwijl u nog typt.

Onderwerpen en afzendernamen in bijvoorbeeld het Japans worden nu correct weergegeven; eerder opgehaalde berichten herstellen zich bij de volgende volledige synchronisatie.
```

**Deutsch**

```
Auf iPhone und iPad ziehen Sie die Liste jetzt nach unten, um zu synchronisieren. Die Posteingänge Ihrer Konten bilden jetzt eine Gruppe "Alle Konten", die Sie öffnen und schließen können.

Ein Newsletter, der für einen breiteren Bildschirm gemacht wurde, passt sich jetzt dem Lesebereich an, statt am rechten Rand abgeschnitten zu werden, und das bei jeder Lesebreite.

Benachrichtigungen über neue E-Mails zeigen jetzt auch den Anfang der Nachricht, nach Absender und Betreff.

Eine geöffnete Nachricht lässt sich jetzt als .eml-Datei sichern, über das neue Menü am Ende der Leseleiste.

Suchergebnisse werden jetzt zu Konversationen gruppiert, wenn die Konversationsgruppierung aktiv ist.

Auf iPhone und iPad scrollen Absender, Empfänger und Anhänge jetzt mit der Nachricht mit, während Antworten, Archivieren und Löschen oben stehen bleiben.

Sie können eine Nachricht jetzt per Fingergeste zoomen, und jede Nachricht öffnet wieder in ihrem eigenen Maßstab.

Eine Liste oder Auswahl mit einem Element zeigt jetzt "1 Unterhaltung" statt "1 Unterhaltungen".

Beim Weiterleiten einer Nachricht stehen ihre Anhänge jetzt im Editor, wo Sie entfernen können, was Sie nicht mitsenden möchten.

Ein Ordner mit einer ungelesenen Nachricht verwendet im Französischen jetzt die richtige Einzahlform.

E-Mail-Konten bei JMAP-Anbietern, die die Einrichtung an einen separaten Server weiterleiten, etwa Thundermail, lassen sich jetzt hinzufügen.

Das Herunterziehen zum Synchronisieren verbirgt das Suchfeld nicht mehr und lässt die Nachrichten nicht mehr springen, und das Menü der Liste ist weg: beide Optionen stehen in den Einstellungen.

Das Zusammenziehen im Kalender auf iPhone und iPad zoomt wieder, sowohl die Stunden als auch die Tage.

Eine geöffnete Nachricht passt auf dem iPhone jetzt auf den Bildschirm: Datum, Aktionen und die Nachricht selbst laufen nicht mehr über die Ränder hinaus.

Post, die in einem selbst geöffneten Ordner eingeht, erscheint jetzt beim Aktualisieren, und die Nachrichtenvorschau erscheint, sobald die Nachrichten geladen sind.

Newsletter, die sich an kleine Bildschirme anpassen, tun dies jetzt auch in der Leseansicht, statt auf dem Telefon am rechten Rand abgeschnitten zu werden.

Suchergebnisse springen nicht mehr zwischen dem richtigen Treffer und einer unpassenden Liste hin und her, während Sie noch tippen.

Betreffzeilen und Absendernamen etwa in japanischer Schrift werden jetzt korrekt angezeigt; zuvor abgerufene Nachrichten korrigieren sich bei der nächsten vollständigen Synchronisierung.
```

**Français**

```
Sur iPhone et iPad, vous tirez désormais la liste vers le bas pour synchroniser. Les boîtes de réception de vos comptes forment désormais un groupe « Tous les comptes » que vous pouvez ouvrir et fermer.

Une lettre d'information conçue pour un écran plus large s'adapte désormais au volet de lecture au lieu de déborder à droite, quelle que soit la largeur à laquelle vous la lisez.

Les notifications de nouveaux messages montrent désormais le début du message, après l'expéditeur et l'objet.

Un message ouvert peut désormais être enregistré au format .eml, depuis le nouveau menu à la fin de la barre de lecture.

Les résultats de recherche sont désormais regroupés en conversations lorsque le regroupement par conversation est activé.

Sur iPhone et iPad, l'expéditeur, les destinataires et les pièces jointes défilent désormais avec le message, tandis que répondre, archiver et supprimer restent en haut.

Vous pouvez désormais zoomer sur un message par pincement, et chaque message s'ouvre de nouveau à sa propre échelle.

Une liste ou une sélection ne contenant qu'un élément affiche désormais "1 conversation" et non "1 conversations".

Le transfert d'un message place désormais ses pièces jointes dans la fenêtre de rédaction, où vous pouvez retirer celles que vous préférez ne pas transmettre.

Un dossier ne contenant qu'un message non lu affiche désormais « 1 non lu » et non « 1 non lus ».

Les comptes de messagerie chez les fournisseurs JMAP qui confient la configuration à un serveur distinct, comme Thundermail, peuvent désormais être ajoutés.

Tirer vers le bas pour synchroniser ne masque plus le champ de recherche ni ne fait sauter les messages, et le menu de la liste a disparu : ses deux options sont dans les Réglages.

Le pincement sur le calendrier de l'iPhone et de l'iPad zoome de nouveau, aussi bien les heures que les jours.

Un message ouvert tient désormais dans l'écran sur iPhone : la date, les actions et le message lui-même ne débordent plus des bords.

Le courrier qui arrive dans un dossier que vous avez ouvert vous-même apparaît désormais lors d'une actualisation, et les aperçus de messages s'affichent dès que les messages sont téléchargés.

Les lettres d'information qui s'adaptent aux petits écrans le font désormais dans la vue de lecture, au lieu d'être coupées à droite sur un téléphone.

Les résultats de recherche n'alternent plus entre la bonne réponse et une liste sans rapport pendant que vous tapez.

Les objets et les noms d'expéditeurs écrits par exemple en japonais s'affichent désormais correctement ; les messages déjà récupérés se corrigent à la prochaine synchronisation complète.
```

**Español**

```
En el iPhone y el iPad ahora tiras de la lista hacia abajo para sincronizar. Las bandejas de entrada de tus cuentas forman ahora un grupo "Todas las cuentas" que puedes abrir y cerrar.

Un boletín pensado para una pantalla más ancha ahora se adapta al panel de lectura en lugar de salirse por la derecha, sea cual sea el ancho con el que lo lea.

Las notificaciones de correo nuevo ahora muestran cómo empieza el mensaje, después del remitente y el asunto.

Un mensaje abierto ya se puede guardar como archivo .eml, desde el nuevo menú al final de la barra de lectura.

Los resultados de búsqueda ahora se agrupan en conversaciones cuando la agrupación por conversación está activada.

En el iPhone y el iPad, el remitente, los destinatarios y los archivos adjuntos ahora se desplazan con el mensaje, mientras que responder, archivar y eliminar se quedan arriba.

Ahora puede acercar o alejar un mensaje pellizcando, y cada mensaje vuelve a abrirse a su propia escala.

Una lista o selección con un solo elemento ahora muestra "1 conversación" en lugar de "1 conversaciones".

Al reenviar un mensaje, sus archivos adjuntos aparecen ahora en el editor, donde puede quitar los que prefiera no enviar.

Una carpeta con un solo mensaje sin leer ahora usa el singular correcto en francés.

Las cuentas de correo de proveedores JMAP que dirigen la configuración a un servidor distinto, como Thundermail, ya se pueden añadir.

Tirar hacia abajo para sincronizar ya no oculta el campo de búsqueda ni desplaza los mensajes, y el menú de la lista ha desaparecido: sus dos opciones están en los Ajustes.

Pellizcar el calendario en el iPhone y el iPad vuelve a ampliarlo, tanto las horas como los días.

Un mensaje abierto ya cabe en la pantalla en el iPhone: la fecha, las acciones y el propio mensaje ya no se salen de los bordes.

El correo que llega a una carpeta que has abierto tú aparece ahora al actualizar, y las vistas previas de los mensajes se muestran en cuanto se descargan.

Los boletines que se adaptan a una pantalla pequeña ahora lo hacen en la vista de lectura, en lugar de quedar cortados por la derecha en el teléfono.

Los resultados de búsqueda ya no alternan entre la respuesta correcta y una lista sin relación mientras sigue escribiendo.

Los asuntos y los nombres de remitente escritos, por ejemplo, en japonés ya se muestran correctamente; los mensajes ya descargados se corrigen en la próxima sincronización completa.
```

**Italiano**

```
Su iPhone e iPad ora trascini l'elenco verso il basso per sincronizzare. Le caselle di posta in arrivo dei tuoi account formano ora un gruppo "Tutti gli account" che puoi aprire e chiudere.

Una newsletter pensata per uno schermo più largo ora si adatta al riquadro di lettura invece di uscire dal bordo destro, a qualunque larghezza la si legga.

Le notifiche di posta in arrivo ora mostrano anche l'inizio del messaggio, dopo il mittente e l'oggetto.

Un messaggio aperto ora può essere salvato come file .eml, dal nuovo menu alla fine della barra di lettura.

I risultati di ricerca ora sono raggruppati in conversazioni quando il raggruppamento per conversazione è attivo.

Su iPhone e iPad il mittente, i destinatari e gli allegati ora scorrono insieme al messaggio, mentre rispondi, archivia ed elimina restano in alto.

Ora si può ingrandire un messaggio con un pizzico, e ogni messaggio torna ad aprirsi alla propria scala.

Un elenco o una selezione con un solo elemento ora mostra "1 conversazione" anziché "1 conversazioni".

Inoltrando un messaggio, i suoi allegati compaiono ora nell'editor, dove è possibile rimuovere quelli che si preferisce non inviare.

Una cartella con un solo messaggio da leggere ora usa il singolare corretto in francese.

Gli account di posta su provider JMAP che affidano la configurazione a un server separato, come Thundermail, ora possono essere aggiunti.

Trascinare verso il basso per sincronizzare non nasconde più il campo di ricerca né fa sobbalzare i messaggi, e il menu dell'elenco non c'è più: entrambe le opzioni sono nelle Impostazioni.

Il pizzico sul calendario di iPhone e iPad torna a ingrandirlo, sia le ore sia i giorni.

Un messaggio aperto ora sta nello schermo su iPhone: la data, le azioni e il messaggio stesso non escono più dai bordi.

La posta che arriva in una cartella aperta da te ora compare con un aggiornamento e le anteprime dei messaggi appaiono appena i messaggi sono scaricati.

Le newsletter che si adattano agli schermi piccoli ora lo fanno anche nella vista di lettura, invece di essere tagliate a destra sul telefono.

I risultati di ricerca non oscillano più fra la risposta giusta e un elenco che non c'entra mentre state ancora digitando.

Gli oggetti e i nomi dei mittenti scritti ad esempio in giapponese ora vengono visualizzati correttamente; i messaggi già scaricati si correggono alla successiva sincronizzazione completa.
```

**Português**

```
No iPhone e no iPad arrasta agora a lista para baixo para sincronizar. As caixas de entrada das suas contas formam agora um grupo "Todas as contas" que pode abrir e fechar.

Uma newsletter feita para um ecrã mais largo passa a adaptar-se ao painel de leitura em vez de sair pela margem direita, seja qual for a largura a que a lê.

As notificações de correio novo passam a mostrar o início da mensagem, depois do remetente e do assunto.

Uma mensagem aberta já pode ser guardada como ficheiro .eml, a partir do novo menu no fim da barra de leitura.

Os resultados da pesquisa passam a ser agrupados em conversas quando o agrupamento por conversa está ativo.

No iPhone e no iPad, o remetente, os destinatários e os anexos passam a deslocar-se com a mensagem, enquanto responder, arquivar e eliminar ficam no topo.

Agora pode ampliar uma mensagem com dois dedos, e cada mensagem volta a abrir à sua própria escala.

Uma lista ou seleção com um só item passa a mostrar "1 conversa" em vez de "1 conversas".

Ao reencaminhar uma mensagem, os seus anexos passam a aparecer no editor, onde pode remover os que prefira não enviar.

Uma pasta com uma só mensagem por ler passa a usar o singular correto em francês.

As contas de correio em fornecedores JMAP que encaminham a configuração para um servidor separado, como o Thundermail, já podem ser adicionadas.

Arrastar para baixo para sincronizar já não esconde o campo de pesquisa nem faz saltar as mensagens, e o menu da lista desapareceu: ambas as opções estão nas Definições.

Juntar os dedos no calendário do iPhone e do iPad volta a ampliá-lo, tanto as horas como os dias.

Uma mensagem aberta agora cabe no ecrã no iPhone: a data, as ações e a própria mensagem já não passam das margens.

O correio que chega a uma pasta que abriu aparece agora ao atualizar, e as pré-visualizações das mensagens surgem assim que as mensagens são transferidas.

As newsletters que se adaptam a ecrãs pequenos passam a fazê-lo na vista de leitura, em vez de ficarem cortadas à direita no telemóvel.

Os resultados da pesquisa deixam de alternar entre a resposta certa e uma lista sem relação enquanto continua a escrever.

Os assuntos e os nomes dos remetentes escritos, por exemplo, em japonês passam a ser apresentados corretamente; as mensagens já descarregadas corrigem-se na próxima sincronização completa.
```

## windows

Paste into: Microsoft Store

Written, not assembled: 17 changes do not fit this store's cap. Every one of them is in the appendix below.

**English**

```
New: search and New Mail have moved to the top of the window, and Sync now sits beside Archive and Delete. Your accounts' inboxes are one "All Accounts" group you can open and shut. New mail raises a desktop notification showing who it is from, what it is about and how the message begins; turn it off in Settings, Notifications. Newsletters written for a wider screen reflow to the reading pane instead of running off the right edge, you can pinch to zoom a message, and every message opens at its own scale again. Search results follow conversation grouping, and an open message can be saved as an .eml file from the new menu at the end of the reading toolbar.

Fixed: forwarding a message keeps its attachments. Search no longer flickers between the right answer and an unrelated list while you type. Subjects and sender names in alphabets such as Japanese read correctly. Mail arriving in a folder you opened yourself shows up on a refresh. Collapsing the folder pane no longer shuts every account's folders for good. A message with no subject is headed in your own language. Accounts on JMAP providers that hand setup to a separate server can be added. Counts of one read as one. And the crash on the launch that offers to make Allodia Mail & Calendar your default mail app is gone.
```

**Nederlands**

```
Nieuw: zoeken en Nieuw bericht staan nu boven in het venster, en Synchroniseren staat naast Archiveren en Verwijderen. De postvakken IN van je accounts vormen één groep "Alle accounts" die je kunt open- en dichtklappen. Nieuwe e-mail geeft een melding op het bureaublad met de afzender, het onderwerp en het begin van het bericht; je zet hem uit bij Instellingen, Meldingen. Nieuwsbrieven die voor een breder scherm zijn gemaakt passen zich aan het leesvenster aan in plaats van rechts weg te lopen, je kunt een bericht zoomen met een knijpbeweging, en elk bericht opent weer op zijn eigen schaal. Zoekresultaten volgen de gespreksweergave, en een geopend bericht kun je opslaan als .eml-bestand via het nieuwe menu aan het eind van de leeswerkbalk.

Opgelost: doorsturen neemt de bijlagen mee. Zoeken springt niet langer heen en weer tussen het juiste antwoord en een lijst die er niets mee te maken heeft. Onderwerpen en afzenders in alfabetten zoals het Japans worden goed weergegeven. E-mail in een map die je zelf opende verschijnt bij het verversen. De mappenbalk dichtklappen sluit niet langer voorgoed de mappen van al je accounts. Een bericht zonder onderwerp krijgt een kop in je eigen taal. Accounts bij JMAP-aanbieders die het instellen aan een aparte server overlaten kunnen worden toegevoegd. Aantallen van één lezen als één. En de crash bij het opstarten waarbij Allodia Mail & Calendar aanbiedt je standaard e-mailprogramma te worden, is verholpen.
```

**Deutsch**

```
Neu: Suche und Neue Nachricht sitzen jetzt oben im Fenster, und Synchronisieren steht jetzt neben Archivieren und Löschen. Die Posteingänge Ihrer Konten bilden eine auf- und zuklappbare Gruppe „Alle Konten“. Neue E-Mail löst eine Benachrichtigung mit Absender, Betreff und dem Anfang der Nachricht aus; abschalten unter Einstellungen, Benachrichtigungen. Newsletter für breitere Bildschirme passen sich dem Lesebereich an, Sie können eine Nachricht mit zwei Fingern zoomen, und jede Nachricht öffnet sich wieder in ihrem eigenen Maßstab. Suchergebnisse folgen der Konversationsansicht, und eine geöffnete Nachricht lässt sich über das neue Menü am Ende der Lese-Symbolleiste als .eml-Datei sichern.

Behoben: Beim Weiterleiten bleiben die Anhänge erhalten. Die Suche springt beim Tippen nicht mehr zwischen dem richtigen Ergebnis und einer unpassenden Liste hin und her. Betreffzeilen und Absendernamen in Alphabeten wie dem Japanischen werden korrekt angezeigt. E-Mail in einem selbst geöffneten Ordner erscheint beim Aktualisieren. Das Zuklappen der Ordnerleiste schließt nicht mehr dauerhaft die Ordner aller Konten. Eine Nachricht ohne Betreff wird in Ihrer eigenen Sprache überschrieben. Konten bei JMAP-Anbietern, die die Einrichtung an einen eigenen Server übergeben, lassen sich hinzufügen. Anzahlen von eins lesen sich als eins. Und der Absturz beim Start, bei dem Allodia Mail & Calendar anbietet, Ihr Standard-E-Mail-Programm zu werden, ist behoben.
```

**Français**

```
Nouveau : la recherche et Nouveau message sont passés en haut de la fenêtre, et Synchroniser est à côté d'Archiver et Supprimer. Les boîtes de réception de vos comptes forment un groupe « Tous les comptes » que vous pouvez replier. Le nouveau courrier déclenche une notification avec l'expéditeur, l'objet et le début du message ; désactivez-la dans Réglages, Notifications. Les infolettres conçues pour un écran plus large s'adaptent au volet de lecture, vous pouvez zoomer un message à deux doigts, et chaque message s'ouvre à sa propre échelle. Les résultats de recherche suivent le regroupement par conversation, et un message ouvert s'enregistre en fichier .eml depuis le nouveau menu au bout de la barre de lecture.

Corrigé : le transfert conserve les pièces jointes. La recherche n'oscille plus entre la bonne réponse et une liste sans rapport pendant la saisie. Les objets et les noms d'expéditeurs dans des alphabets comme le japonais s'affichent correctement. Le courrier arrivé dans un dossier que vous avez ouvert apparaît à l'actualisation. Replier le volet des dossiers ne referme plus définitivement ceux de tous vos comptes. Un message sans objet reçoit un titre dans votre propre langue. Les comptes JMAP dont la configuration passe par un autre serveur peuvent être ajoutés. Les nombres valant un se lisent au singulier. Et le plantage au démarrage, quand Allodia Mail & Calendar propose de devenir votre messagerie par défaut, a disparu.
```

**Español**

```
Novedades: la búsqueda y Nuevo mensaje han pasado a la parte superior de la ventana, y Sincronizar está ahora junto a Archivar y Eliminar. Las bandejas de entrada de tus cuentas forman un grupo "Todas las cuentas" que puedes abrir y cerrar. El correo nuevo muestra una notificación de escritorio con el remitente, el asunto y cómo empieza el mensaje; puedes desactivarla en Ajustes, Notificaciones. Los boletines pensados para una pantalla más ancha se adaptan al panel de lectura en lugar de salirse por la derecha, puedes ampliar un mensaje pellizcando, y cada mensaje vuelve a abrirse con su propia ampliación. Los resultados de búsqueda siguen la agrupación por conversaciones, y un mensaje abierto puede guardarse como archivo .eml desde el nuevo menú al final de la barra de lectura.

Corregido: al reenviar se conservan los adjuntos. La búsqueda ya no salta entre la respuesta correcta y una lista que no viene al caso mientras escribes. Los asuntos y los nombres de remitente en alfabetos como el japonés se leen bien. El correo que llega a una carpeta que abriste tú aparece al actualizar. Plegar el panel de carpetas ya no cierra para siempre las carpetas de todas tus cuentas. Un mensaje sin asunto lleva un encabezado en tu propio idioma. Pueden añadirse cuentas JMAP cuya configuración está en otro servidor. Los recuentos de uno se leen en singular. Y el fallo al arrancar, cuando Allodia Mail & Calendar ofrece convertirse en tu programa de correo predeterminado, ha desaparecido.
```

**Italiano**

```
Novità: la ricerca e Nuovo messaggio si sono spostati in cima alla finestra, e Sincronizza si trova ora accanto ad Archivia ed Elimina. Le caselle di posta in arrivo dei tuoi account formano un gruppo "Tutti gli account" che puoi aprire e chiudere. La nuova posta mostra una notifica sul desktop con mittente, oggetto e l'inizio del messaggio; puoi disattivarla in Impostazioni, Notifiche. Le newsletter pensate per uno schermo più largo si adattano al riquadro di lettura, puoi ingrandire un messaggio a due dita, e ogni messaggio si riapre al proprio ingrandimento. I risultati di ricerca seguono il raggruppamento per conversazione, e un messaggio aperto può essere salvato come file .eml dal nuovo menu in fondo alla barra di lettura.

Risolto: l'inoltro mantiene gli allegati. La ricerca non oscilla più tra la risposta giusta e un elenco che non c'entra mentre digiti. Gli oggetti e i nomi dei mittenti in alfabeti come il giapponese si leggono correttamente. La posta arrivata in una cartella che hai aperto tu compare all'aggiornamento. Chiudere il pannello delle cartelle non chiude più per sempre le cartelle di tutti gli account. Un messaggio senza oggetto ha un'intestazione nella tua lingua. Si possono aggiungere account JMAP la cui configurazione sta su un altro server. I conteggi di uno si leggono al singolare. E l'arresto anomalo all'avvio, quando Allodia Mail & Calendar propone di diventare il tuo programma di posta predefinito, è risolto.
```

**Português**

```
Novo: a pesquisa e Nova mensagem passaram para o topo da janela, e Sincronizar fica agora ao lado de Arquivar e Eliminar. As caixas de entrada das suas contas formam um grupo "Todas as contas" que pode abrir e fechar. O correio novo mostra uma notificação no ambiente de trabalho com o remetente, o assunto e o início da mensagem; pode desativá-la em Definições, Notificações. As newsletters pensadas para um ecrã mais largo ajustam-se ao painel de leitura em vez de saírem pela direita, pode ampliar uma mensagem com dois dedos, e cada mensagem volta a abrir na sua própria ampliação. Os resultados da pesquisa seguem o agrupamento por conversa, e uma mensagem aberta pode ser guardada como ficheiro .eml a partir do novo menu no fim da barra de leitura.

Corrigido: o reencaminhamento mantém os anexos. A pesquisa deixou de oscilar entre a resposta certa e uma lista sem relação enquanto escreve. Os assuntos e os nomes dos remetentes em alfabetos como o japonês leem-se corretamente. O correio que chega a uma pasta que abriu você mesmo aparece ao atualizar. Fechar o painel de pastas já não fecha para sempre as pastas de todas as contas. Uma mensagem sem assunto tem um cabeçalho na sua própria língua. Podem adicionar-se contas JMAP cuja configuração está noutro servidor. As contagens de um leem-se no singular. E a falha no arranque, quando o Allodia Mail & Calendar se oferece para ser o seu programa de correio predefinido, desapareceu.
```

## android

Paste into: Google Play

Written, not assembled: 13 changes do not fit this store's cap. Every one of them is in the appendix below.

**English**

```
New: newsletters fit the reading pane instead of running off the edge, you can pinch to zoom a message, search follows conversation grouping, a message can be saved as an .eml file, and notifications show how the message begins. Fixed: forwarding keeps the attachments, search no longer flickers while you type, subjects in other alphabets read correctly, folders you opened yourself refresh, more JMAP accounts can be added, and counts of one read as one.
```

**Nederlands**

```
Nieuw: nieuwsbrieven passen in het leesvenster, je kunt een bericht zoomen met een knijpbeweging, zoeken volgt de gespreksweergave, een bericht kun je opslaan als .eml-bestand, en meldingen tonen hoe het bericht begint. Opgelost: doorsturen neemt de bijlagen mee, zoeken springt niet meer tijdens het typen, onderwerpen in andere alfabetten worden goed getoond, mappen die je zelf opende worden ververst, meer JMAP-accounts kunnen worden toegevoegd, en aantallen van één lezen als één.
```

**Deutsch**

```
Neu: Newsletter passen in den Lesebereich, Nachrichten lassen sich mit zwei Fingern zoomen, die Suche folgt der Konversationsansicht, eine Nachricht lässt sich als .eml sichern, und Benachrichtigungen zeigen den Nachrichtenanfang. Behoben: Weiterleiten nimmt die Anhänge mit, die Suche springt beim Tippen nicht mehr, Betreffs in anderen Alphabeten werden korrekt angezeigt, selbst geöffnete Ordner aktualisieren sich, mehr JMAP-Konten sind möglich, und Anzahlen von eins lesen sich als eins.
```

**Français**

```
Nouveau : les infolettres tiennent dans le volet de lecture, zoomez un message à deux doigts, la recherche suit le regroupement par conversation, enregistrez un message en .eml, et les notifications montrent le début du message. Corrigé : le transfert conserve les pièces jointes, la recherche n'oscille plus à la saisie, les objets en d'autres alphabets s'affichent bien, vos dossiers ouverts s'actualisent, plus de comptes JMAP sont acceptés, et les comptages de un sont au singulier.
```

**Español**

```
Novedades: los boletines caben en el panel de lectura, puedes ampliar un mensaje pellizcando, la búsqueda sigue la agrupación por conversaciones, un mensaje puede guardarse como .eml, y las notificaciones muestran cómo empieza el mensaje. Corregido: al reenviar se conservan los adjuntos, la búsqueda ya no salta mientras escribes, los asuntos en otros alfabetos se leen bien, las carpetas que abriste tú se actualizan, se admiten más cuentas JMAP, y los recuentos de uno se leen en singular.
```

**Italiano**

```
Novità: le newsletter stanno nel riquadro di lettura, puoi ingrandire un messaggio con due dita, la ricerca segue il raggruppamento per conversazione, un messaggio può essere salvato come .eml, e le notifiche mostrano come inizia il messaggio. Risolto: l'inoltro mantiene gli allegati, la ricerca non oscilla più mentre scrivi, gli oggetti in altri alfabeti si leggono bene, le cartelle che apri tu si aggiornano, si accettano più account JMAP, e i conteggi di uno si leggono al singolare.
```

**Português**

```
Novo: as newsletters cabem no painel de leitura, pode ampliar uma mensagem com dois dedos, a pesquisa segue o agrupamento por conversa, uma mensagem pode ser guardada como .eml, e as notificações mostram como a mensagem começa. Corrigido: o reencaminhamento mantém os anexos, a pesquisa deixou de oscilar enquanto escreve, os assuntos noutros alfabetos leem-se bem, as pastas que abriu são atualizadas, aceitam-se mais contas JMAP, e as contagens de um leem-se no singular.
```

## linux

Paste into: (no store yet)

**English**

```
Search is now centred at the top of the window on Mac, Windows and Linux, and New Mail has moved up there with it. Sync sits beside Archive and Delete, and on iPhone and iPad you pull the list down to sync. Your accounts' inboxes are now one "All Accounts" group you can open and shut.

A newsletter written for a wider screen now reflows to fit the reading pane instead of running off the right edge, at whatever width you read it.

New-mail notifications now show how the message begins, after the sender and the subject.

An open message can now be saved as an .eml file, from the new menu at the end of the reading toolbar.

Search results are now grouped into conversations when conversation grouping is on.

You can now pinch to zoom a message, and every message opens at its own scale again.

A list or a selection holding one thing now reads "1 conversation" rather than "1 conversations".

Forwarding a message now brings its attachments into the composer, where you can remove any you would rather not pass on.

A folder holding one unread message now reads as a singular in French.

Mail accounts on JMAP providers that hand setup to a separate server, such as Thundermail, can now be added.

Starting the app no longer announces the mail that arrived while it was closed: notifications are for mail that arrives while you have it open.

Mail arriving in a folder you opened yourself now shows up on a refresh, and message previews appear as soon as the bodies are downloaded.

Newsletters that adapt to a small screen now do so in the reading view, instead of being cut off at the right edge on a phone.

Search results no longer flicker between the right answer and an unrelated list while you are still typing.

Subjects and sender names in alphabets such as Japanese now read correctly; mail synced earlier corrects itself on the next full sync.

Your software centre now shows what changed in each release, and lists the Linux app as one that works with a touchscreen.

The buttons that minimise, maximise and close the window sit in the top corner again, and the application's name no longer stands above the message you are reading.
```

**Nederlands**

```
Zoeken staat nu gecentreerd bovenaan het venster op de Mac, Windows en Linux, en Nieuwe e-mail is meeverhuisd naar boven. Synchroniseren staat naast Archiveren en Verwijderen, en op de iPhone en iPad trek je de lijst naar beneden om te synchroniseren. De postvakken IN van je accounts vormen nu één groep "Alle accounts" die je kunt openen en sluiten.

Een nieuwsbrief die voor een breder scherm is gemaakt, past zich nu aan het leesvenster aan in plaats van weg te vallen aan de rechterkant, op elke breedte waarop u leest.

Meldingen van nieuwe e-mail tonen nu ook hoe het bericht begint, na de afzender en het onderwerp.

Een geopend bericht kan nu worden opgeslagen als .eml-bestand, via het nieuwe menu aan het eind van de leesbalk.

Zoekresultaten worden nu gegroepeerd in gesprekken wanneer groeperen op gesprek aanstaat.

U kunt nu met een knijpbeweging in- en uitzoomen op een bericht, en elk bericht opent weer op zijn eigen schaal.

Een lijst of selectie met één item toont nu "1 gesprek" in plaats van "1 gesprekken".

Bij het doorsturen van een bericht staan de bijlagen nu meteen in het opstelvenster, waar u kunt weglaten wat u liever niet meestuurt.

Een map met één ongelezen bericht gebruikt nu de juiste enkelvoudsvorm in het Frans.

E-mailaccounts bij JMAP-aanbieders die het instellen doorverwijzen naar een aparte server, zoals Thundermail, kunnen nu worden toegevoegd.

Bij het starten meldt de app niet langer alles wat binnenkwam terwijl hij dicht was: meldingen gaan over post die binnenkomt terwijl hij openstaat.

Post die binnenkomt in een map die je zelf hebt geopend, verschijnt nu bij een vernieuwing, en berichtvoorbeelden verschijnen zodra de berichten zijn opgehaald.

Nieuwsbrieven die zich aanpassen aan een klein scherm doen dat nu ook in de leesweergave, in plaats van op een telefoon rechts weg te vallen.

Zoekresultaten wisselen niet langer tussen het juiste antwoord en een lijst die er niets mee te maken heeft terwijl u nog typt.

Onderwerpen en afzendernamen in bijvoorbeeld het Japans worden nu correct weergegeven; eerder opgehaalde berichten herstellen zich bij de volgende volledige synchronisatie.

Je softwarecentrum laat nu zien wat er in elke versie is gewijzigd, en vermeldt de Linux-app als een app die met een touchscreen werkt.

De knoppen om het venster te minimaliseren, te maximaliseren en te sluiten staan weer in de bovenhoek, en de naam van de toepassing staat niet langer boven het bericht dat je leest.
```

**Deutsch**

```
Die Suche steht jetzt zentriert am oberen Fensterrand auf dem Mac, unter Windows und Linux, und Neue E-Mail ist mit nach oben gewandert. Synchronisieren sitzt neben Archivieren und Löschen, und auf iPhone und iPad ziehen Sie die Liste nach unten, um zu synchronisieren. Die Posteingänge Ihrer Konten bilden jetzt eine Gruppe "Alle Konten", die Sie öffnen und schließen können.

Ein Newsletter, der für einen breiteren Bildschirm gemacht wurde, passt sich jetzt dem Lesebereich an, statt am rechten Rand abgeschnitten zu werden, und das bei jeder Lesebreite.

Benachrichtigungen über neue E-Mails zeigen jetzt auch den Anfang der Nachricht, nach Absender und Betreff.

Eine geöffnete Nachricht lässt sich jetzt als .eml-Datei sichern, über das neue Menü am Ende der Leseleiste.

Suchergebnisse werden jetzt zu Konversationen gruppiert, wenn die Konversationsgruppierung aktiv ist.

Sie können eine Nachricht jetzt per Fingergeste zoomen, und jede Nachricht öffnet wieder in ihrem eigenen Maßstab.

Eine Liste oder Auswahl mit einem Element zeigt jetzt "1 Unterhaltung" statt "1 Unterhaltungen".

Beim Weiterleiten einer Nachricht stehen ihre Anhänge jetzt im Editor, wo Sie entfernen können, was Sie nicht mitsenden möchten.

Ein Ordner mit einer ungelesenen Nachricht verwendet im Französischen jetzt die richtige Einzahlform.

E-Mail-Konten bei JMAP-Anbietern, die die Einrichtung an einen separaten Server weiterleiten, etwa Thundermail, lassen sich jetzt hinzufügen.

Beim Start meldet die App nicht mehr alles, was eingegangen ist, während sie geschlossen war: Benachrichtigungen gelten für Post, die bei geöffneter App eintrifft.

Post, die in einem selbst geöffneten Ordner eingeht, erscheint jetzt beim Aktualisieren, und die Nachrichtenvorschau erscheint, sobald die Nachrichten geladen sind.

Newsletter, die sich an kleine Bildschirme anpassen, tun dies jetzt auch in der Leseansicht, statt auf dem Telefon am rechten Rand abgeschnitten zu werden.

Suchergebnisse springen nicht mehr zwischen dem richtigen Treffer und einer unpassenden Liste hin und her, während Sie noch tippen.

Betreffzeilen und Absendernamen etwa in japanischer Schrift werden jetzt korrekt angezeigt; zuvor abgerufene Nachrichten korrigieren sich bei der nächsten vollständigen Synchronisierung.

Ihr Software-Center zeigt jetzt, was sich in jeder Version geändert hat, und führt die Linux-App als App auf, die mit einem Touchscreen funktioniert.

Die Schaltflächen zum Minimieren, Maximieren und Schließen des Fensters sitzen wieder in der oberen Ecke, und der Name der Anwendung steht nicht mehr über der gelesenen Nachricht.
```

**Français**

```
La recherche est désormais centrée en haut de la fenêtre sur Mac, Windows et Linux, et Nouveau message l'y a rejointe. Synchroniser se trouve à côté d'Archiver et de Supprimer, et sur iPhone et iPad vous tirez la liste vers le bas pour synchroniser. Les boîtes de réception de vos comptes forment désormais un groupe « Tous les comptes » que vous pouvez ouvrir et fermer.

Une lettre d'information conçue pour un écran plus large s'adapte désormais au volet de lecture au lieu de déborder à droite, quelle que soit la largeur à laquelle vous la lisez.

Les notifications de nouveaux messages montrent désormais le début du message, après l'expéditeur et l'objet.

Un message ouvert peut désormais être enregistré au format .eml, depuis le nouveau menu à la fin de la barre de lecture.

Les résultats de recherche sont désormais regroupés en conversations lorsque le regroupement par conversation est activé.

Vous pouvez désormais zoomer sur un message par pincement, et chaque message s'ouvre de nouveau à sa propre échelle.

Une liste ou une sélection ne contenant qu'un élément affiche désormais "1 conversation" et non "1 conversations".

Le transfert d'un message place désormais ses pièces jointes dans la fenêtre de rédaction, où vous pouvez retirer celles que vous préférez ne pas transmettre.

Un dossier ne contenant qu'un message non lu affiche désormais « 1 non lu » et non « 1 non lus ».

Les comptes de messagerie chez les fournisseurs JMAP qui confient la configuration à un serveur distinct, comme Thundermail, peuvent désormais être ajoutés.

Au démarrage, l'application n'annonce plus les messages arrivés pendant qu'elle était fermée : les notifications concernent le courrier qui arrive pendant qu'elle est ouverte.

Le courrier qui arrive dans un dossier que vous avez ouvert vous-même apparaît désormais lors d'une actualisation, et les aperçus de messages s'affichent dès que les messages sont téléchargés.

Les lettres d'information qui s'adaptent aux petits écrans le font désormais dans la vue de lecture, au lieu d'être coupées à droite sur un téléphone.

Les résultats de recherche n'alternent plus entre la bonne réponse et une liste sans rapport pendant que vous tapez.

Les objets et les noms d'expéditeurs écrits par exemple en japonais s'affichent désormais correctement ; les messages déjà récupérés se corrigent à la prochaine synchronisation complète.

Votre logithèque affiche désormais ce qui a changé dans chaque version et présente l'application Linux comme compatible avec un écran tactile.

Les boutons de réduction, d'agrandissement et de fermeture de la fenêtre se trouvent de nouveau dans le coin supérieur, et le nom de l'application ne figure plus au-dessus du message que vous lisez.
```

**Español**

```
La búsqueda aparece ahora centrada en la parte superior de la ventana en Mac, Windows y Linux, y Nuevo correo se ha trasladado allí con ella. Sincronizar está junto a Archivar y Eliminar, y en el iPhone y el iPad tiras de la lista hacia abajo para sincronizar. Las bandejas de entrada de tus cuentas forman ahora un grupo "Todas las cuentas" que puedes abrir y cerrar.

Un boletín pensado para una pantalla más ancha ahora se adapta al panel de lectura en lugar de salirse por la derecha, sea cual sea el ancho con el que lo lea.

Las notificaciones de correo nuevo ahora muestran cómo empieza el mensaje, después del remitente y el asunto.

Un mensaje abierto ya se puede guardar como archivo .eml, desde el nuevo menú al final de la barra de lectura.

Los resultados de búsqueda ahora se agrupan en conversaciones cuando la agrupación por conversación está activada.

Ahora puede acercar o alejar un mensaje pellizcando, y cada mensaje vuelve a abrirse a su propia escala.

Una lista o selección con un solo elemento ahora muestra "1 conversación" en lugar de "1 conversaciones".

Al reenviar un mensaje, sus archivos adjuntos aparecen ahora en el editor, donde puede quitar los que prefiera no enviar.

Una carpeta con un solo mensaje sin leer ahora usa el singular correcto en francés.

Las cuentas de correo de proveedores JMAP que dirigen la configuración a un servidor distinto, como Thundermail, ya se pueden añadir.

Al iniciarse, la aplicación ya no anuncia el correo que llegó mientras estaba cerrada: las notificaciones son para el correo que llega con la aplicación abierta.

El correo que llega a una carpeta que has abierto tú aparece ahora al actualizar, y las vistas previas de los mensajes se muestran en cuanto se descargan.

Los boletines que se adaptan a una pantalla pequeña ahora lo hacen en la vista de lectura, en lugar de quedar cortados por la derecha en el teléfono.

Los resultados de búsqueda ya no alternan entre la respuesta correcta y una lista sin relación mientras sigue escribiendo.

Los asuntos y los nombres de remitente escritos, por ejemplo, en japonés ya se muestran correctamente; los mensajes ya descargados se corrigen en la próxima sincronización completa.

Tu centro de software ahora muestra qué ha cambiado en cada versión y presenta la aplicación para Linux como compatible con una pantalla táctil.

Los botones de minimizar, maximizar y cerrar la ventana vuelven a estar en la esquina superior, y el nombre de la aplicación ya no aparece sobre el mensaje que estás leyendo.
```

**Italiano**

```
La ricerca è ora centrata in cima alla finestra su Mac, Windows e Linux, e Nuovo messaggio si è spostato lassù con lei. Sincronizza si trova accanto ad Archivia ed Elimina, e su iPhone e iPad trascini l'elenco verso il basso per sincronizzare. Le caselle di posta in arrivo dei tuoi account formano ora un gruppo "Tutti gli account" che puoi aprire e chiudere.

Una newsletter pensata per uno schermo più largo ora si adatta al riquadro di lettura invece di uscire dal bordo destro, a qualunque larghezza la si legga.

Le notifiche di posta in arrivo ora mostrano anche l'inizio del messaggio, dopo il mittente e l'oggetto.

Un messaggio aperto ora può essere salvato come file .eml, dal nuovo menu alla fine della barra di lettura.

I risultati di ricerca ora sono raggruppati in conversazioni quando il raggruppamento per conversazione è attivo.

Ora si può ingrandire un messaggio con un pizzico, e ogni messaggio torna ad aprirsi alla propria scala.

Un elenco o una selezione con un solo elemento ora mostra "1 conversazione" anziché "1 conversazioni".

Inoltrando un messaggio, i suoi allegati compaiono ora nell'editor, dove è possibile rimuovere quelli che si preferisce non inviare.

Una cartella con un solo messaggio da leggere ora usa il singolare corretto in francese.

Gli account di posta su provider JMAP che affidano la configurazione a un server separato, come Thundermail, ora possono essere aggiunti.

All'avvio l'applicazione non annuncia più la posta arrivata mentre era chiusa: le notifiche riguardano la posta che arriva mentre è aperta.

La posta che arriva in una cartella aperta da te ora compare con un aggiornamento e le anteprime dei messaggi appaiono appena i messaggi sono scaricati.

Le newsletter che si adattano agli schermi piccoli ora lo fanno anche nella vista di lettura, invece di essere tagliate a destra sul telefono.

I risultati di ricerca non oscillano più fra la risposta giusta e un elenco che non c'entra mentre state ancora digitando.

Gli oggetti e i nomi dei mittenti scritti ad esempio in giapponese ora vengono visualizzati correttamente; i messaggi già scaricati si correggono alla successiva sincronizzazione completa.

Il tuo centro software ora mostra che cosa è cambiato in ogni versione e presenta l'applicazione per Linux come compatibile con uno schermo touch.

I pulsanti per ridurre a icona, ingrandire e chiudere la finestra sono di nuovo nell'angolo superiore e il nome dell'applicazione non compare più sopra il messaggio che stai leggendo.
```

**Português**

```
A pesquisa aparece agora centrada no topo da janela no Mac, no Windows e no Linux, e Nova mensagem subiu para lá com ela. Sincronizar fica junto de Arquivar e Eliminar, e no iPhone e no iPad arrasta a lista para baixo para sincronizar. As caixas de entrada das suas contas formam agora um grupo "Todas as contas" que pode abrir e fechar.

Uma newsletter feita para um ecrã mais largo passa a adaptar-se ao painel de leitura em vez de sair pela margem direita, seja qual for a largura a que a lê.

As notificações de correio novo passam a mostrar o início da mensagem, depois do remetente e do assunto.

Uma mensagem aberta já pode ser guardada como ficheiro .eml, a partir do novo menu no fim da barra de leitura.

Os resultados da pesquisa passam a ser agrupados em conversas quando o agrupamento por conversa está ativo.

Agora pode ampliar uma mensagem com dois dedos, e cada mensagem volta a abrir à sua própria escala.

Uma lista ou seleção com um só item passa a mostrar "1 conversa" em vez de "1 conversas".

Ao reencaminhar uma mensagem, os seus anexos passam a aparecer no editor, onde pode remover os que prefira não enviar.

Uma pasta com uma só mensagem por ler passa a usar o singular correto em francês.

As contas de correio em fornecedores JMAP que encaminham a configuração para um servidor separado, como o Thundermail, já podem ser adicionadas.

Ao arrancar, a aplicação deixa de anunciar o correio que chegou enquanto esteve fechada: as notificações são para o correio que chega com ela aberta.

O correio que chega a uma pasta que abriu aparece agora ao atualizar, e as pré-visualizações das mensagens surgem assim que as mensagens são transferidas.

As newsletters que se adaptam a ecrãs pequenos passam a fazê-lo na vista de leitura, em vez de ficarem cortadas à direita no telemóvel.

Os resultados da pesquisa deixam de alternar entre a resposta certa e uma lista sem relação enquanto continua a escrever.

Os assuntos e os nomes dos remetentes escritos, por exemplo, em japonês passam a ser apresentados corretamente; as mensagens já descarregadas corrigem-se na próxima sincronização completa.

O seu centro de software passa a mostrar o que mudou em cada versão e apresenta a aplicação para Linux como compatível com um ecrã tátil.

Os botões para minimizar, maximizar e fechar a janela estão novamente no canto superior, e o nome da aplicação já não aparece por cima da mensagem que está a ler.
```

## Changes in this release

Engineering commentary, carried over from each fragment. Never pasted into a store.

### Clear a search from the field on macOS — `clear-a-search-from-the-field-on-macos` (macos, minor)

> The three other desktop and phone fields carry a clear button from their platform control
> (`AutoSuggestBox`, `gtk::SearchEntry`, SwiftUI's `.searchable`); the wide macOS layout uses a
> plain `TextField` in the list header and had none, so leaving search meant selecting the text
> and deleting it. The button empties the field and nothing else: `onChange` already dispatches
> the clear, so the core keeps one route in and rule 6 (clearing resets the scope) holds without
> a second call site.

### New Mail, Sync and search where you reach for them — `new-mail-and-search-in-the-window-top-row` (macos, ios, windows, linux, minor)

> Four moves, one shape, taken from Outlook because that is what people arrive from. **New Mail**
> (was "Compose") leaves the footer nobody looked at for the top of the window: on macOS and Linux a
> window-toolbar item in the leading corner, which is the only surface there that is the window's
> own top row, so the traffic lights' space is reserved by the toolbar rather than guessed at; on
> Windows the head of the actions row, because the folder pane it would otherwise sit in collapses
> to an icon strip and nothing else in the app writes a message. The search field is centred over
> the window, which is honest about its reach: the default scope is every account and every folder,
> so it was a control drawn over one column while acting on all of them. **Sync** (was "Refresh")
> joins the actions bar past a divider, never disabled, because it acts on the mailbox rather than
> on the selection; the footer it left is now a status line alone. iPhone and iPad get the gesture
> instead, and drop the menu item they had. And the unified list becomes an **All Accounts** group
> with the Inbox under it, which is the shape a unified Sent and Drafts need; its expansion is the
> core's and persisted, like every account's.
>
> The pane's rules 16 and 17 and the bar's Sync are on Apple, Windows and Linux. Android remains
> recorded under the contracts' Known gaps.

### Newsletters reflow to the reading pane — `newsletters-reflow-to-the-reading-pane` (macos, ios, windows, android, linux, minor)

> Half of real mail has no `@media` rules at all: a table pinned to 600px in the markup and again
> inline, its cells pinned to a third of that each. It is now reflowed to the pane rather than cut
> off at the right edge, in the **shared document** (`crates/mailcal-app/src/html/reflow.rs`), so
> it is one implementation and every client has it.
>
> The approach is the mail clients' own, the "munger" that came from AOSP Email through K-9 Mail
> and Thunderbird for Android and that Infomaniak's iOS client still runs: reflow before scaling
> anything, because a reflowed message is readable at the reader's own text size while a scaled one
> is a small picture of a message. Where ours differs is that it is CSS rather than script, so it
> needs neither a script in the message (`rendering-security.md` gates 1 and 2) nor the pane's
> width, which the core does not have. `max-width:100%` is unconditional because it can only
> shrink; clearing the fixed widths off a table's cells sits behind a media query whose breakpoint
> is the widest fixed width the message asks for **plus the page's own inset on both edges**, so the
> engine re-decides on every resize with nothing re-rendered. Two traps have seeded fixtures because
> each is the other's regression test: the selector asks for a width attribute that is not a
> percentage, or a full-bleed `<table width="100%" bgcolor>` band would stop spanning the pane; and
> the breakpoint carries the padding, or there is a 28px band of pane widths, which a 13-inch iPad
> lands in, where a message is clipped and nothing fires.

### Notifications show how the message begins — `notifications-show-how-the-message-begins` (ios, android, linux, minor)

> `NewMailPreview` gains the body snippet the list row already carries, so a notification and its
> row cannot quote the message differently and no second extraction exists to disagree with the
> first. It is empty until the body sync has run (an IMAP account commits headers first), and each
> client drops the line rather than drawing a blank one, which is the two-line notification they
> all had before. The slots differ per platform and the order does not: Windows has three text
> elements, Android puts the snippet on the expanded `BigTextStyle`, iOS and the Linux portal give
> one body that the subject and the snippet share. A per-account overflow summary names no single
> message, so it quotes none. `docs/privacy-policy.md` moves with it, in both locales: it
> enumerates what a lock-screen preview contains, and that enumeration is now longer. Windows is
> not on the `Platforms:` line because it gains notifications in this same release: its own note
> already describes all three lines, and a second bullet repeating one of them would read as two
> changes to a store's reader.

### Save a message as an .eml file — `save-a-message-as-an-eml-file` (macos, ios, windows, android, linux, minor)

> The reading view's action row gains a menu at its end, after archive and delete, holding the
> actions on the message as a document rather than on its place in the mailbox
> (`docs/reading-actions.md`). Its first item writes the message out as a `.eml`. The file is the
> raw source the engine cached, byte for byte, so it opens elsewhere as the message the sender
> actually sent: a file rebuilt from the reading view would carry our headers, our part order and
> our transfer encodings, and no signature over the original would still verify. The engine gained
> `Engine::message_source` for it, since every existing read over that blob hands back a decoding
> of it. The name comes from the core, from the subject the client displays, so a subject holding
> a slash or a right-to-left override cannot decide where the file lands.

### Search follows conversation grouping — `search-follows-conversation-grouping` (macos, ios, windows, android, linux, minor)

> Search projected flat rows whatever the list was set to, so turning conversations on changed
> every list in the app except this one. It now goes through the same threaded projection the
> mailbox list uses, which needed no new ordering or grouping code: that projection already reads
> `AccountMessage::in_scope` as "belongs to the list being built", so a search marks its hits in
> scope and the rest of their conversations out of it. Three properties fall out of that one
> reading. A conversation is listed only if a message in it matched; it carries the whole thread,
> so a row means here what it means in the mailbox (`docs/list-selection.md`); and it is labelled
> by, and sorted on, its newest **matching** message, so an unrelated later reply neither carries
> an old thread to the top nor puts a subject on the row that answers nothing the user asked. The
> MCP surface stays flat on purpose: it answers with messages, paged by offset and addressed by
> key, and a conversation is not addressable that way.

### The message header scrolls with the message — `the-message-header-scrolls-with-the-message` (ios, minor)

> Who sent a message, who else got it and what it carries belong to the mail rather than to the
> pane, so on iPhone and iPad they now go up with the body and only the action row stays
> ([`docs/reading-actions.md`](../../reading-actions.md)). The reading view is one `ScrollView`
> rather than a header over a body that scrolls itself: the web view is laid out at the whole
> message's height, which WebKit reports, so it has nothing left to scroll and the one scroll moves
> both. A header drawn *over* the body looks identical until a finger lands on it, because that
> view takes the drag, and a message whose header fills the screen (an invitation card, or twenty
> attachments) could then not be scrolled at all. Laying the web view out at the message's height
> is also what lets WebKit apply its own fit to an over-wide message
> ([`docs/reading-zoom.md`](../../reading-zoom.md)); held to the pane's height it does not.
> `ReadingHeaderScrollTests` holds it, on the invitation, whose card is the one header in the
> showcase dataset guaranteed to outgrow a large phone.

### New-mail notifications on Windows — `windows-new-mail-notifications` (windows, minor)

> The Windows client now raises a desktop notification when its live runtime commits inbound Inbox
> mail, through the Windows App SDK's `AppNotificationManager`, which registers in both build
> shapes (MSIX identity when packaged, an AppUserModelId for the running executable when not). It
> uses the same `collect_cached_new_mail` seam Linux does, called off the `MailboxList` signal, so
> there is no second network schedule and the cadence stays whatever the account's push/poll
> setting is. One notification per message, keyed by the stable message key, grouped per account;
> keying per account instead would let the next pass replace a message the user had not seen, after
> the marks had already moved past it. What a pass says is `NewMailNotices`, which is WinUI- and
> catalog-free and unit-tested; the toast itself is a thin adapter. Settings gains the
> Notifications category the taxonomy has always reserved for slot 7, with the shared catalog's
> copy. Clicking a notification brings the app forward; opening the message it names is a
> follow-up.

### You can zoom a message — `you-can-zoom-a-message` (macos, ios, windows, android, linux, minor)

> The zoom half of `docs/reading-zoom.md`, whose sizing half is now
> `newsletters-reflow-to-the-reading-pane.md`. The shared reading document declared
> `initial-scale=1`, which pins the page at 1:1 and is the condition under which Blink declines to
> shrink an over-wide message; it now names a width and no scale, and Android honours the viewport
> at all (`useWideViewPort` is false by default, which ignores the tag outright). Zoom itself is now
> on every client, from each message's own scale up to 5×, starting again at that scale on every
> open, so every host whose zoom is the *view's* rather than the *page's* resets it. On iPhone and
> iPad the range starts at 1 rather than 0.25, which is WebKit's and not ours: it will not scale a
> page below the width at which its layout viewport fits.

### Collapsing the sidebar keeps your folder trees — `collapsing-the-sidebar-keeps-your-folder-trees` (windows, patch)

> A NavigationView shuts every hierarchical item on its way to the icon strip, and reports it
> through the same two-way `IsExpanded` binding a chevron click writes to. The shell could not tell
> the two apart, so it told the core, which persisted it: the pane came back with every account
> shut, and stayed that way across launches, because what is stored is the set of accounts the user
> collapsed (`docs/folder-pane.md`, rules 2 to 4).
>
> The discriminator is the pane itself: a write that arrives while `IsPaneOpen` is false is the
> pane's, not the user's, and a compact pane has no chevron to click, so nothing real is lost by
> ignoring every one of them. Reopening then re-asserts the trees from the core, since the
> framework does not put back what it shut.
>
> Caught by the actions bar's new case, which collapses the pane to prove New Mail survives it, and
> left the harness account shut for every later suite. `Mailcal.Tests` cannot see it: the binding
> that misreports is WinUI's, so the regression test is in `uitests/FolderPane.Tests.ps1`.

### Counts of one read as one — `counts-of-one-read-as-one` (macos, ios, windows, android, linux, patch)

> The catalog had no plural mechanism, so every `{count}` message carried a single hard-plural
> form and a folder holding one thing said "1 conversations". `mailcal-l10n` now folds a
> `<key>_one` partner into the plural's accessor, which picks the form the active locale's
> grammar asks for; the partner gets no accessor of its own, so a caller passes a count and the
> four clients cannot disagree about the rule. No client code changed. French takes the singular
> at zero as well as one (CLDR), which is reachable: an empty folder still states its count.
> Deliberately narrow: only the eight keys that can actually render one are paired (the three
> mailbox counts, the two selection counts, the three reminder offsets); month counts never reach
> one, since `SYNC_DEPTHS` starts at three. The older pairs that spell the numeral into the
> sentence ("1 attendee") are two messages a call site chooses between, not plural forms, and are
> left alone; carrying the `{count}` is what separates the two, and a test pins it.

### Forwarding keeps the attachments — `forward-carries-the-attachments` (macos, ios, windows, android, linux, patch)

> A forward rendered the composer document and nothing else, so the original's attachments were
> left behind: the recipient got the quoted text of a message whose files were the point of
> forwarding it, and neither end could see that anything was missing. The core now stages them
> (`stage_forwarded_attachments`, the engine's own attachment list, so a quoted body's inline
> `cid:` images are not duplicated as files and an invitation's own `text/calendar` part is still
> not one) into a directory the client names, and each client seeds its composer's attachment list
> from the answer. They arrive as [`ComposerFileAttachment`]s, the shape a picked file and a shared
> one already take, so the list, the remove button and the submit path are the ones that already
> existed on all four clients. Two rules are worth keeping: the composer opens **after** staging,
> because one on screen holding nothing can be sent in the window before the files arrive; and
> files that cannot be read open the composer with its error line set, because an empty attachment
> list is a claim that the message had nothing attached.

### A French folder badge of one reads as one — `french-unread-count-of-one` (macos, ios, windows, android, linux, patch)

> `a11y_unread_count` is the folder pane's unread badge, and it was the ninth key of the shape the
> catalog's plural forms were built for. The eight paired then did not include it, so a folder
> holding a single unread message announced "1 non lus". French is the only shipped language whose
> wording here agrees with the number; the other six word the badge without agreement, so their
> singular is the same sentence and is written as such. The badge is spoken rather than drawn (the
> number alone is what the eye sees), so this reaches a screen reader and nothing else.

### JMAP setup that moves to another server — `jmap-setup-that-moves-to-another-server` (macos, ios, windows, android, linux, patch)

> RFC 8620 discovery starts at the user's own domain, and a hosted provider routinely redirects
> that to the host actually running the server. The engine's session walker resolved each hop's
> `Location` against the configured base rather than against the URL that issued it, and put it
> through `SessionUrlPolicy` on the way, which forced every target back onto the origin the walk
> started from. A cross-origin hop therefore resolved to the URL it came from, the chain
> oscillated in place until the hop budget ran out, and the user saw "too many session redirects".
> Fixing the walk alone was not enough: that same base is what the session's advertised `apiUrl`
> resolves against, so after an origin change every method call would have been aimed at a host
> that never had the session. The engine now resolves a `Location` against the URL that issued it
> (RFC 9110), refuses a hop that leaves TLS since these requests carry the account's credentials,
> and hands the session the URL that actually served it. Nothing changed in this repository beyond
> the engine pin: autodetect deliberately stores the typed domain rather than an ephemeral
> redirect target, and that decision holds now that the engine follows the chain correctly.

### Notifications skip the catch-up a launch begins with — `notifications-skip-the-catch-up-at-startup` (linux, patch)

> `collect_cached_new_mail` reported everything past the stored high-water mark, and a desktop
> launch opens with a catch-up sync, so starting the app after it had been closed for a while
> announced every message that had arrived meanwhile, each one already visible in the list behind
> the notifications. The core now floors a cached scan at the instant it was built: mail older than
> the session advances the mark and is never reported. The floor covers a whole session rather than
> one pass, which is why it could not be expressed as a mark, and it sits in the core rather than
> in each host, so every desktop inherits it. `run_background_sync` deliberately keeps no floor: a
> mobile pass exists to report exactly the mail that arrived while nobody was looking.

### A steadier mailbox screen on the phone — `phone-mailbox-chrome` (ios, patch)

> The message list was a row inside a `VStack`, so it was not the scroll view the navigation bar
> sizes itself against. The search field became pinned chrome that hid itself the moment the
> refresh control moved the content offset: a pull dropped the field and threw every row up by its
> height, then dropped them back when the sync finished. The list is now the navigation stack's
> content, with the folder name as a large title, which is what puts the title and the field inside
> the scroll view; the horizon line, the selection bar and the sync strip are safe-area insets, so
> they draw where stack rows drew them without standing between the list and the bar. The list's
> overflow menu went with it: conversation grouping and Reset database both live in Settings, and
> the menu was the last of the surfaces that predated them.

### Pinch the calendar on iPhone and iPad — `pinch-the-calendar-on-iphone-and-ipad` (ios, patch)

> The grid's pinch recognizer sat on the SwiftUI overlay that carries it. An overlay is a sibling of
> the content rather than its ancestor, and UIKit offers a touch only to the recognizers of the
> hit-test view and the views above it in that chain, so the handler was never called: the view was
> built, the recognizer existed, and the grid drew perfectly while ignoring every pinch. The scroll
> catcher beside it already hangs its recognizer on the window for exactly this reason; the zoom
> catcher now does the same, narrowed to the grid in `gestureRecognizerShouldBegin` so a pinch on
> the mailbox list still belongs to the mailbox list. Nothing but a real gesture can see this
> failure, which is why it survived every gate the repository had: `CalendarPinchTests` in the new
> Apple UI suite is the one that now holds it.

### The open message fits the screen — `reading-pane-fits-the-phone` (ios, patch)

> A compact width drew the six action buttons icon-only at the platform's largest control size, and
> six of those are wider than any iPhone. A row that outgrows its pane is not clipped on its own:
> it sets the width of the column it sits in, so the header, the invitation card and the body all
> hung off both edges of the display, taking the date and the overflow button with them. The
> control size is now measured the way the labels already were (`ViewThatFits` offers the next size
> down, so nothing is written down per device), the touch target rides on the glyph's own box so a
> smaller control is not a smaller target, and the pane's edge inset is one value the header,
> recipients, action row, attachment bar, invitation card and plain-text body all read.

### Refreshing a folder you opened yourself — `refreshing-a-folder-you-opened-yourself` (macos, ios, windows, android, linux, patch)

> An account binds a provider to INBOX and to the folders the server tagged with SPECIAL-USE;
> every other folder is downloaded once, when it is opened, and was then in no account pass and
> under no IDLE watch. Standing in one, a pull, a poll tick and a return to network all
> republished the rows already on screen, and restarting the app was the only way to see new
> mail in it. A pass now re-syncs the folder on screen when it is one the account binds no
> provider to. Separately, the body-warm pass is what gives a row its preview snippet on a
> provider that sends none (IMAP: the snippet is derived from the body): it wrote the snippets to
> the store and stopped there, so the rows kept the blank second line they were published with
> until something else happened to rebuild the list. It now rebuilds once at the end of a pass
> that warmed anything.

### Responsive newsletters on a phone — `responsive-newsletters-on-a-phone` (macos, ios, windows, android, linux, patch)

> The sanitiser kept `class` but dropped `id`, and a newsletter's mobile layout routinely hangs off
> `id` alone: the container table carries a fixed `width="600"` and only
> `table[id=templateContainer]{width:100% !important}`, inside a `max-width:480px` `@media` block,
> narrows it. With the attribute gone that rule matched nothing, so the message laid out 600px wide
> in a phone's ~384px viewport and every line of text ran off the right edge while images were cut
> in half. `id` is now kept, unprefixed, so the message's own responsive CSS resolves. The desktop
> reading pane is wide enough that those rules never applied there, which is why this only showed
> on a phone.

### Search answers the query you finished typing — `search-answers-the-query-you-finished-typing` (macos, ios, windows, android, linux, patch)

> Intents are spawned, not queued, so typing a word ran a search per keystroke concurrently, and
> they finished by wildly different margins: a two-letter prefix matches half the mailbox and
> resolves every hit from the store, measured at 1.8 s against a tenth of a second for the
> finished query. `rebuild_snapshot` published unconditionally, so the screen went to whichever
> finished last, reliably the broadest and least useful one, and nothing corrected it until an
> unrelated rebuild landed: on the reported device, not until a new message arrived. The core now
> carries `App::search_generation`, the same guard contacts search has had: a rebuild reads it
> with the query it is answering and re-checks it before publishing, dropping a list a newer
> search has superseded. macOS and Windows also gained the 250 ms debounce Android and Linux
> already had, which is latency rather than correctness: the two halves do not substitute for
> each other, since searches 250 ms apart still overlap whenever one is slow.

### Subjects in other alphabets read correctly — `subjects-in-other-alphabets-read-correctly` (macos, ios, windows, android, linux, patch)

> A Japanese subject and sender name arrived as `$B...(B` in the notification and the list
> row. Header text is RFC 2047 encoded, and the engine's IMAP adapter read every charset it
> did not know as UTF-8: `ISO-2022-JP` is 7-bit, so that read produces no replacement
> character to notice, just the escape sequences as text. The surveyed sibling was worse:
> nothing decoded Gmail's headers at all, since its API returns them raw, so a non-ASCII
> Gmail subject showed the literal `=?iso-2022-jp?b?...?=`. Both now go through one decoder
> in `engine-mime` over `mail-parser`'s charset table. JMAP and Graph were never affected;
> their servers decode. The decode happens as mail is synced, so text already stored stays
> wrong until that folder is synced again, which is what the note says.

### An untitled message is headed in your own language on Windows — `untitled-message-header-in-your-language` (windows, patch)

> The Windows reading pane wrote its "(no subject)" placeholder as an English literal rather than
> reading `mail_no_subject` from the catalog, so a message with a blank subject was headed in
> English in all seven languages. Found while wiring the `.eml` export, which names the file after
> the subject the pane *displays* (`docs/reading-actions.md`): left as it was, an untitled message
> would also have exported under an English name on Windows alone.

### What changed, under every version in your software centre — `what-changed-in-each-linux-release` (linux, patch)

> The metainfo listed every release as a version and a date and nothing else, so a software centre
> drew "No details for this release" under all twelve of them. The copy had existed all along: a
> released note carries the same per-locale bullets the stores are given, so the generator now reads
> its `linux` section through `changelog_fragments.parse_release` and emits a `<description>`, one
> `<p>` for a release that changed one thing and a `<ul>` for one that changed several, each
> translation interleaved after the paragraph it translates so AppStream's locale fallback lands on
> the right original. A release with no `linux` section keeps the bare `<release>` it had: it shipped
> on other platforms only, and another platform's copy would describe a build this reader could not
> install.
>
> Two claims in the same file were also wrong about the app. `<control>touch</control>` was missing,
> and a control left out is read as one the app cannot be used with rather than one left unstated;
> the controls move to `<supports>`, because a *recommended* keyboard is what filed a tablet as
> incompatible. And `display_length` defaults to `side="shortest"`, so the bare 960 claimed that much
> *height* and put every 1366x768 laptop outside what the app recommends. `appstreamcli
> check-syscompat` went from two incompatible chassis to none.

### Window buttons in the corner — `window-buttons-in-the-corner` (linux, patch)

> The mail surface's actions bar spans the list and the reading pane, so it is the window's top row
> and the reading pane's header sits a row below it. The controls rode that header, which left close
> a row short of the corner a pointer is thrown at, and the header, having no title widget of its
> own, fell back to the window's title and stood the application's name over the message being read.
> The controls now ride the actions bar, which is a `GtkWindowHandle` so the row drags and
> double-clicks like the caption row it is, and neither that header nor the contacts detail beside it
> names anything. Every other page already carries the controls on its rightmost header, which is
> that page's own top row.

### The default-mail-app offer no longer crashes the Windows app at startup — `windows-offer-no-longer-crashes-at-startup` (windows, patch)

> The offer is raised by the account-list signal, which `MainWindow` subscribes inside its own
> constructor, so an account that connects in tens of milliseconds (a local server, or a primed
> cached snapshot) got there before the window had a visual tree. `ContentDialog.ShowAsync` on an
> unrooted element throws "This element does not have a XamlRoot" out of an `async void`, which
> reaches no catch and takes the process down: one launch in five, and only while the question was
> still unanswered, so it hid behind having been asked once. The offer now waits for `Loaded` and
> is put then, and `DialogHelper` refuses an unrooted dialog outright so no future caller can
> bring the crash back. The rule is `DefaultMailApp.WhenToAsk`, which is WinUI-free and therefore
> unit-tested; `docs/client-traps.md` carries the trap.
