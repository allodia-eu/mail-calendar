# Linux: an icon beside each Settings category, and a real one on an empty folder

Platforms: linux
Bump: patch

> The Settings sidebar was the one surface drawing no icons at all; it now carries the same
> meanings the other platforms draw, from Adwaita where it has them and from GNOME's
> icon-development-kit where it does not. The empty-folder page named an icon Adwaita 50 does not
> ship, so it drew GTK's broken-image placeholder; it now shows the inbox tray, as Apple does.

**English**

```
On Linux, each Settings category now has an icon beside its name, and an empty folder shows an inbox instead of a broken image.
```

**Nederlands**

```
Op Linux staat nu naast elke categorie in Instellingen een pictogram, en een lege map toont een postvak in plaats van een kapotte afbeelding.
```

**Deutsch**

```
Unter Linux hat jetzt jede Kategorie in den Einstellungen ein Symbol neben ihrem Namen, und ein leerer Ordner zeigt einen Posteingang statt eines defekten Bildes.
```

**Français**

```
Sous Linux, chaque catégorie des Réglages a désormais une icône à côté de son nom, et un dossier vide affiche une boîte de réception au lieu d'une image cassée.
```

**Español**

```
En Linux, cada categoría de Ajustes tiene ahora un icono junto a su nombre, y una carpeta vacía muestra una bandeja de entrada en lugar de una imagen rota.
```

**Italiano**

```
Su Linux ogni categoria delle Impostazioni ha ora un'icona accanto al nome, e una cartella vuota mostra una posta in arrivo invece di un'immagine danneggiata.
```

**Português**

```
No Linux, cada categoria das Definições tem agora um ícone ao lado do nome, e uma pasta vazia mostra uma caixa de entrada em vez de uma imagem danificada.
```
